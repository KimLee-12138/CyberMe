---
id: extract-co-5aa0cefd
type: extract
status: extracted
course: CO
source:
  - "[[91_Raw-Archive/PDF/CO_计组还原卷_2024计组_修正版.pdf_课件_未知日期_5aa0cefd.pdf]]"
source_pages: all
source_hash: "5aa0cefd3523c2e1c8ee9dd6675cddffda5af2541c3397c097af5ff5d0b71432"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 计组还原卷_2024计组_修正版.pdf：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

## 第1页

2024 年《计算机组成原理》计组卷还原版
（修正版） 
本版根据  2024 截图、老师录音和新上传  PPT 修正。
重点修正：磁盘填空、 beq 控制信号、 ori 零扩展、主存扩展片选逻辑。
一、填空题  
1. 带符号整数表示与范围  
题目：
在计算机中带符号整数通常使用 表示，如果有四位，表示带符号整数的范围 到 。
答案：
解析：
n 位补码范围：
4 位补码：
2. 磁盘基础概念  
题目：
一条条同心圆是 ，磁盘上访问信息的最小物理单位是 ，硬盘和内存进行数据交换一般采用 方式。
修正答案：
解析：
上一版把第一空写成 “ 柱面 ” 不准确。
正确区分：补码；-8 到  +7
-2^(n-1) ~ 2^(n-1)-1
-2^3 ~ 2^3-1 = -8 ~ +7
磁道；扇区； DMA

## 第2页

名词 含义
磁道 单个盘面上的一圈同心圆
柱面 多个盘面上半径相同的一组磁道
扇区 磁盘读写的最小物理单位
DMA 硬盘和主存之间大批量数据交换常用方式
校验码 作用
奇偶校验 简单检错
CRC 检错能力强，常用于通信和磁盘
海明码 可纠正一位错误3. 数据校验码  
题目：
数据校验码主要有奇偶校验码、 、 。
答案：
解析：
4. 浮点数对阶与  IEEE 754 表示  
题目：
f = 1.0 + 4.5 ，计算中对阶的作用是使 相等，计算出  f 用  16 进制表示为 。
答案：
解析：
IEEE 754 单精度：
组合：CRC 校验码；海明校验码
阶码；40B00000H
1.0 + 4.5 = 5.5
5.5 = 101.1B = 1.011 × 2^2
S = 0
E = 127 + 2 = 129 = 10000001B
M = 01100000000000000000000

## 第3页

5. ALU 提高加法速度的进位方式  
题目：
ALU 实现了 进位方式，提高了速度。
答案：
解析：
串行进位要逐位等待低位进位，速度慢。先行进位利用进位生成函数  G 和进位传递函数  P 提前并行计
算进位。
常见关系：
二、分析题  
题 1 ：主存容量扩展、地址分配与片选逻辑  
原题还原  
使用半导体存储器进行容量扩展，按字节编址：
要求：给出地址分配和片选逻辑。
（1 ）计算容量和芯片数量  
ROM 区：
所以：0 10000001 01100000000000000000000
= 0100 0000 1011 0000 0000 0000 0000 0000
= 40B00000H
先行进位 / 并行进位
G_i = X_i · Y_i
P_i = X_i + Y_i
C_{i+1} = G_i + P_i C_i
0000H - 07FFH 为  ROM 区，选用  EPROM 芯片， 2KB/ 片；
0800H - 13FFH 为  RAM 区，选用  RAM 芯片， 2KB/ 片和  1KB/ 片；
地址总线 A15 ~ A0 。
07FFH - 0000H + 1 = 0800H = 2048B = 2KB
ROM：2KB EPROM 1 片

## 第4页

区域 地址范围 容量 片内地址
ROM0 0000H - 07FFH 2KB A10 ~ A0
RAM0 0800H - 0FFFH 2KB A10 ~ A0
RAM1 1000H - 13FFH 1KB A9 ~ A0RAM 区：
所以：
（2 ）地址分配  
总范围：
共 5KB ，需要覆盖到  13FFH ，因此至少需要  13 根地址线：
高位：
地址安排：
（3 ）片选逻辑  
按老师  PPT 的写法，低位地址给芯片，高位地址做片选。若片选高有效：
同时还应满足：
写完整一点：13FFH - 0800H + 1 = 0C00H = 3072B = 3KB
RAM：2KB RAM 1 片  + 1KB RAM 1 片
0000H - 13FFH
A12 ~ A0
A15 A14 A13 = 000
CS0_ROM  = /A12 · /A11
CS1_RAM2K = /A12 · A11
CS2_RAM1K = A12 · /A11 · /A10
A15 = 0， A14 = 0 ， A13 = 0

## 第5页

注意：
如果题目芯片片选端是低有效，则逻辑要整体取反或画成带反相泡的形式。考试一般看你能否说明 “ 高
位译码形成片选 ” 。
题 2 ： MIPS 指令格式  
原题还原  
给出  MIPS 三类指令格式：
R 型：
I 型：
J 型：
（1 ） R 型最多多少条指令？最多多少个通用寄存器？ I 型 /J
型最多多少条指令？ 
答案：
若 op=000000 表示  R 型，则具体功能由  funct 决定：
寄存器编号  5 位：
I 型和  J 型  op 字段  6 位：
若考虑  R 型已经占用一个  op 编码，则非  R 型  op 还剩  63 种。答题时看题目问 “ 字段能表示多少 ” 还是
“ 排除  R 型后还剩多少 ” 。CS0_ROM   = /A15 · /A14 · /A13 · /A12 · /A11
CS1_RAM2K = /A15 · /A14 · /A13 · /A12 ·  A11
CS2_RAM1K = /A15 · /A14 · /A13 ·  A12 · /A11 · /A10
op | rs | rt | rd | shamt | funct
6    5    5    5     5       6
op | rs | rt | immediate
6    5    5      16
op | target address
6       26
R 型最多 2^6 = 64 种功能
最多 2^5 = 32 个通用寄存器
理论最多 2^6 = 64 种操作码

## 第6页

部件 功能
PC 保存下一条或当前指令地址
指令存储器 根据  PC 取出指令
寄存器组 读源寄存器，写目的寄存器
扩展器  Ext 把 16 位立即数扩展为  32 位
ALU 加减、逻辑运算、地址计算、比较
数据存储器 为 lw/sw 读写数据
控制器 根据  op/funct 产生控制信号
MUX 根据控制信号选择数据来源
Next Addr Logic 计算  PC 的下一个值（2 ）给机器码判断功能  
标准解题模板：
常见  RTL ：
三、设计与计算题  
题 1 ： CPU 数据通路与控制信号  
（1 ） CPU 结构图中常见部件及功能  
（2 ）根据执行过程判断指令  
截图中过程为：1. 先看 op。
2. op=000000 → R 型，看  funct 。
3. op 为  lw/sw/beq/ori/j 等  → 按对应格式拆字段。
4. 写出寄存器编号、立即数或目标地址。
5. 写 RTL 功能。
add: R[rd] ← R[rs] + R[rt]
sub: R[rd] ← R[rs] - R[rt]
ori: R[rt] ← R[rs] OR ZeroExt(imm16)
lw : R[rt] ← M[R[rs] + SignExt(imm16)]
sw : M[R[rs] + SignExt(imm16)] ← R[rt]
beq: if R[rs] = R[rt] then PC ← PC + 4 + SignExt(imm16) × 4
j  : PC ← {PC+4 高  4 位 , target26, 00}

## 第7页

指 令 MemtoReg RegWr MemWr RegDst ExtOp ALUSrc Branch Jump ALUOp
R-type
add/sub0 1 0 1 x 0 0 0001 /
由
funct
决 定
ori 0 1 0 0 0 1 0 0 010
lw 1 1 0 0 1 1 0 0 000
sw x 0 1 x 1 1 0 0 000
beq x 0 0 x x 0 1 0 100
jump x 0 0 x x x 0 1 xxx答案：
功能：
执行过程：
（3 ）七类指令控制信号表（按老师  PPT 修正）  
重点解释：
ori 使用零扩展： ExtOp=0 。
lw/sw 计算地址，要使用符号扩展： ExtOp=1 。
beq 的 ALU 只做  R[rs]-R[rt] 判断  Zero ， ALU 第二输入来自  busB ，所以在该  PPT 数据通路中  
ExtOp=x 。
jump 只改  PC ，不写寄存器，不写内存。
（4 ） ALU 控制器的作用  
答案：
主控制单元根据  op 判断指令大类，产生大部分控制信号和  ALUOp ；若是  R 型指令，还要结合  funct 
字段，由  ALU 局部控制器产生具体  ALUctr ，决定  ALU 执行  add 、 sub 、 and 、 or 、 slt 等操作。M[PC]                                      取指
Addr ← R[rs] + SignExt(imm16)              计算地址
Mem[Addr] ← R[rt]                          存储
PC ← PC + 4                                更新  PC
sw rt, imm(rs)
M[R[rs] + SignExt(imm16)] ← R[rt]
1. IR ← M[PC]
2. 读 R[rs] 和  R[rt] ， imm16 符号扩展
3. Addr ← R[rs] + SignExt(imm16)
4. M[Addr] ← R[rt]
5. PC ← PC + 4

## 第8页

（5 ） PC ←  PC + 4 与  PC31:2  + 1  
答案：
MIPS 指令长度为  32 位，即  4 字节。按字节编址时，下一条指令地址为：
老师数据通路图中若写：
是因为指令地址低两位固定为  00 ，只对高  30 位 “ 指令字序号 ” 加  1 ，本质等价于字节地址加  4 。
题 2 ： Cache 四路组相联映射  
（1 ）四路组相联查找过程  
地址划分：
查找：
（2 ） Cache 总容量  
若题目问 “Cache 总容量多少位 ” ，通常包括：
如果只问 “Cache 数据容量 ” ，则只算：
（3 ）脏位  Dirty bit  
答案：
脏位表示  Cache 块是否被修改过。PC ← PC + 4
PC<31:2> ← PC<31:2> + 1
Tag + 组号  Index + 块内偏移  Offset
1. 用 Index 找到  Cache 中对应的组；
2. 在该组  4 路中同时比较  Tag ；
3. 某一路  Valid=1 且  Tag 相等  → 命中；
4. 否则缺失，从主存调入对应块。
数据位 + Tag 位  + Valid 位  + Dirty 位  + 替换控制位
行数 × 块大小

## 第9页

虚拟存储中的页表项也有类似的修改位 / 脏位。页面换出时，如果脏位为  1 ，必须写回磁盘。
四、论述题  
题 1 ：虚拟存储  + TLB + Cache 访存过程  
答案：
题 2 ：理论和实验学了什么  
答案示例：
理论学习了数据表示、 ALU 、指令系统、 CPU 数据通路、控制器、微程序、异常中断、 Cache 、虚拟存
储和  I/O 。
实验中通过  Verilog/VHDL 、 Quartus 、 ModelSim 、 SignalTap 或  Logisim 观察硬件模块如何组成  
CPU ，理解控制信号如何驱动数据通路执行指令。
2024 卷复习重点  Dirty=0：与主存一致，替换时可直接丢弃；
Dirty=1：已被修改，替换时必须先写回主存。
CPU 给出虚拟地址  VA
↓
查 TLB
↓
TLB 命中：得到物理地址  PA
TLB 缺失：访问页表
    ↓
    页表项有效：更新  TLB ，得到  PA
    页表项无效：缺页中断
        ↓
        操作系统从磁盘调入页面
        若主存无空闲页框，需要页面置换
        若换出页为脏页，先写回磁盘
        更新页表和  TLB
↓
用 PA 查 Cache
↓
Cache 命中：直接访问  Cache
Cache 缺失：
    ↓
    从主存调入一块
    若 Cache 无空闲块，需要替换
    若被替换块为脏块，先写回主存
↓
CPU 得到数据

## 第10页

1. 补码范围
2. IEEE 754 单精度表示
3. 磁道、扇区、 DMA
4. 主存扩展和片选逻辑
5. MIPS R/I/J 指令格式
6. sw/lw/add/ori/beq/jump 执行过程
7. 控制信号表
8. Cache 映射和地址划分
9. 虚拟存储访存流程

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_计算机组成原理_课程MOC|计算机组成原理 MOC]]

---
id: extract-co-bee330d7
type: extract
status: extracted
course: CO
source:
  - "[[91_Raw-Archive/DOC/CO_06_第七章 层次结构存储系统（下）.md_笔记_未知日期_bee330d7.md]]"
source_pages: all
source_hash: "bee330d72abdb8de6e1171c48891937e55d0361acc17b6fc3d576ffa53a75c2b"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 06_第七章 层次结构存储系统（下）.md：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

# 第七章 层次结构存储系统（下）

> cover:小孩不哭站起来lu

## 复习大纲

- 存储器概述和RAM芯片
- 主存与CPU的连接及其读写操作
- 高速缓冲存储器（cache）
- 虚拟存储器

## 命中率、缺失率与访存速度

![img](C:\Users\22067\Desktop\计组复习\assets\cover553_20260603100054.jpg)

- 命中 Hit：CPU 要访问的数据在 Cache 中。

- 缺失 Miss：CPU 要访问的数据不在 Cache 中，需要访问主存。

命中率为 H，则缺失率为 1 - H。

设：

- H 为 Cache 命中率；
- TH 为 Cache 命中访问时间；
- TM 为访问主存的额外时间。

平均访问时间：

- T = HTH + (1 - H)(TH + TM)

化简得：

- T = TH + (1 - H)TM

含义：

- 每次访问都先查 Cache，所以至少花 TH；
- 如果缺失，还要额外访问主存，额外代价为 TM；
- 缺失发生的概率是 1 - H。

例如：
H=0.85，TH=1ns，TM=20ns：
T = 1 + 0.15×20 = 4ns

H=0.95：
T = 1 + 0.05×20 = 2ns

H=0.99：
T = 1 + 0.01×20 = 1.2ns

## 缺失率和关联度

![img](C:\Users\22067\Desktop\计组复习\assets\cover557_20260603100054.jpg)

关联度是指一个主存块调入 Cache 时，可以存放的 Cache 位置个数。

三种映射方式的关联度：
1. 直接映射：关联度为 1。每个主存块只能放到固定的一行。
2. 全相联映射：关联度为 Cache 行数。每个主存块可以放到任意一行。
3. N 路组相联映射：关联度为 N。每个主存块只能进入固定组，但可放在组内任意一行。

关联度与性能的关系：
关联度越高，主存块可选择的位置越多，冲突减少，缺失率降低，命中率提高。
但关联度越高，需要比较的 Tag 越多，硬件复杂度增加，命中时间可能变长。

因此：
直接映射：命中时间最短，但命中率较低；
全相联映射：命中率较高，但命中时间较长；
组相联映射：在命中率和命中时间之间折中，实际使用较多。

## 替换算法——先进先出（FIFO-简）

![img](C:\Users\22067\Desktop\计组复习\assets\cover566_20260603100055.jpg)

FIFO 替换算法又称先进先出算法。

基本思想：
当 Cache 某组已满且需要调入新块时，淘汰最早进入该组的块。

FIFO 只根据块进入 Cache 的先后顺序进行替换，不考虑该块最近是否被访问过。
命中时，FIFO 一般不改变块的先后顺序。

优点：
实现简单，硬件开销较小。

缺点：
最早进入的块可能仍然被频繁使用，但仍会被淘汰；
因此命中率不一定高，甚至可能出现 Cache 容量增大但缺失次数反而增加的情况，即 Belady 异常。

例：
地址序列为 1 2 3 4 1 2 5 1 2 3 4 5。
使用 FIFO 时，3 行/组可能比 4 行/组缺失更少，说明 FIFO 不一定随容量增大而变好。

## 替换算法——最近最少用

![img](C:\Users\22067\Desktop\计组复习\assets\cover568_20260603100055.jpg)

![img](C:\Users\22067\Desktop\计组复习\assets\cover569_20260603100055.jpg)

LRU 是最近最少使用替换算法。

基本思想：
当 Cache 某组已满且需要调入新块时，淘汰最近最久没有被访问的那一块。

LRU 与 FIFO 的区别：
FIFO 按进入 Cache 的先后顺序淘汰；
LRU 按最近使用情况淘汰。
LRU 更符合程序的时间局部性，因此通常命中率更高。

LRU 是一种栈算法，命中率一般随组容量增大而提高。

当程序集中访问的块数超过 Cache 组容量时，会出现频繁替换，命中率很低，这种现象称为颠簸或 Thrashing。
例如 Cache 每组只有 3 行，但访问序列为 1,2,3,4,1,2,3,4,...，由于需要反复访问 4 个块，而 Cache 只能保存 3 个块，因此每次都可能缺失。

LRU 实现时通常不是移动 Cache 块，而是为每一行设置 LRU 位或计数器，记录各块最近使用情况。替换时淘汰 LRU 值表示“最久未使用”的那一行。

## 写策略（Cache一致性问题）

### 一、为什么会有 Cache 一致性问题？

前面我们说过，Cache 是主存部分内容的副本。

比如主存中有一个数据：

```
主存 A = 100
```

它被调入 Cache 后：

```
Cache A = 100
主存 A = 100
```

这时候二者一致，没问题。

但是如果 CPU 修改了 Cache 中的 A：

```
Cache A = 200
主存 A = 100
```

这时候就出现问题了：

> Cache 和主存中的同一个数据不一样了。

这就是 **Cache 一致性问题**。

------

### 二、哪些情况会导致一致性问题？

PPT 说主要有两类情况。

#### 1. 多个设备都能访问主存

比如 I/O 设备可以直接读写内存。

如果 CPU 修改了 Cache，但没有及时写回主存：

```
Cache A = 200
主存 A = 100
```

这时 I/O 设备去主存读 A，它读到的是旧值 `100`，就是错的。

反过来，如果 I/O 设备修改了主存：

```
主存 A = 300
Cache A = 100
```

CPU 再从 Cache 读 A，就会读到旧值 `100`，也错了。

------

#### 2. 多个 CPU 各自有 Cache，共享主存

比如多核 CPU 中，每个核可能都有自己的 Cache。

假设 CPU1 和 CPU2 都缓存了同一个数据 A：

```
CPU1 Cache A = 100
CPU2 Cache A = 100
主存 A = 100
```

如果 CPU1 修改了自己的 Cache：

```
CPU1 Cache A = 200
CPU2 Cache A = 100
主存 A = 100
```

那么 CPU2 的 Cache 和主存都变成旧数据了。

这也是 Cache 一致性问题。

------

### 三、写操作有两种情况

PPT 说写操作分为：

```
写命中
写不命中
```

------

#### 1. 写命中

写命中就是：

> CPU 要写的那个数据已经在 Cache 中。

例如 CPU 要修改 A，Cache 里正好有 A。

```
CPU 写 A
Cache 中有 A
```

这就是写命中。

这时问题是：

> CPU 修改 Cache 后，要不要马上同步修改主存？

对应两种策略：

```
写直达 / 直写
写回 / 回写
```

------

#### 2. 写不命中

写不命中就是：

> CPU 要写的那个数据不在 Cache 中。

例如 CPU 要修改 A，但是 Cache 中没有 A。

```
CPU 写 A
Cache 中没有 A
```

这就是写不命中。

这时问题是：

> 要不要先把 A 所在的主存块调入 Cache，再写？

对应两种策略：

```
写分配
非写分配
```

------

### 四、写命中的两种策略

#### 1. 写直达 / 直写 Write Through

PPT 写：

```
同时写 Cache 和主存单元，超级慢。
```

意思是：

CPU 写命中时，既修改 Cache，也立即修改主存。

例如：

```
原来：
Cache A = 100
主存 A = 100

CPU 写 A = 200

写直达后：
Cache A = 200
主存 A = 200
```

优点是：

```
Cache 和主存始终一致
控制简单
```

缺点是：

```
每次写都要访问主存
速度慢
```

因为主存比 Cache 慢很多，所以写直达性能较差。

------

#### 2. 写回 / 回写 Write Back

PPT 写：

```
只写 Cache 不写主存，缺失时该块一次写回，每行设一个修改位。
```

意思是：

CPU 写命中时，只修改 Cache，不马上修改主存。

例如：

```
原来：
Cache A = 100
主存 A = 100

CPU 写 A = 200

写回后：
Cache A = 200
主存 A = 100
```

这时候 Cache 和主存暂时不一致。

为了记录这个 Cache 行被修改过，需要设置一个：

```
修改位 / 脏位 Dirty Bit
```

例如：

```
Dirty = 1
```

表示：

> 这一行 Cache 的内容已经被 CPU 修改过，和主存不一致，将来替换出去前必须写回主存。

等这块数据被替换时，再一次性写回主存：

```
Cache A = 200 写回主存
主存 A = 200
Dirty 清 0
```

优点是：

```
速度快
减少主存写次数
```

缺点是：

```
控制复杂
要维护修改位
Cache 和主存可能暂时不一致
```

------

### 五、写不命中的两种策略

写不命中时，Cache 里没有要写的数据。

这时有两种处理方式。

------

#### 1. 写分配 Write Allocate

PPT 写：

```
写主存单元，然后主存块装入 Cache
```

意思是：

CPU 写的数据不在 Cache 中，就先把这个数据所在的主存块调入 Cache，然后再写 Cache。

流程可以理解为：

```
1. 发现写不命中
2. 从主存把目标块调入 Cache
3. 在 Cache 中修改目标数据
```

如果配合写回策略，后续修改可以先留在 Cache 中，等替换时再写回。

写分配适合和：

```
写回 Write Back
```

一起使用。

因为既然之后可能还会继续写这个数据，先调进 Cache，后面写起来就快。

------

#### 2. 非写分配 No Write Allocate

PPT 写：

```
直接写主存单元，不把主存块装入到 Cache
```

意思是：

CPU 写的数据不在 Cache 中，就直接写主存，不调入 Cache。

流程：

```
1. 发现写不命中
2. 直接把数据写入主存
3. Cache 不变化
```

非写分配适合和：

```
写直达 Write Through
```

一起使用。

因为写直达本来就要写主存，所以写不命中时直接写主存就行，不一定要污染 Cache。

------

### 六、四种策略放在一起理解

可以这样记：

| 情况     | 策略     | 做法                       | 特点           |
| -------- | -------- | -------------------------- | -------------- |
| 写命中   | 写直达   | 同时写 Cache 和主存        | 一致性好，但慢 |
| 写命中   | 写回     | 只写 Cache，替换时写回主存 | 快，但控制复杂 |
| 写不命中 | 写分配   | 先把块调入 Cache，再写     | 适合写回       |
| 写不命中 | 非写分配 | 直接写主存，不调入 Cache   | 适合写直达     |

------

### 七、最常见的组合

一般常见组合有两种：

#### 组合一：写直达 + 非写分配

```
写命中：Cache 和主存同时写
写不命中：直接写主存，不调入 Cache
```

特点：

```
一致性好
实现简单
但写速度慢
```

------

#### 组合二：写回 + 写分配

```
写命中：只写 Cache，设置脏位
写不命中：先调入 Cache，再写 Cache
替换时：如果脏位为 1，则写回主存
```

特点：

```
性能好
减少主存写次数
但控制复杂
```

现代处理器中经常采用类似写回 + 写分配的策略。

------

### 八、脏位 Dirty Bit 是什么？

写回策略中，每个 Cache 行通常需要一个修改位，也叫脏位。

```
Dirty = 0：这一行没有被修改，和主存一致
Dirty = 1：这一行被修改过，和主存不一致
```

如果替换时：

```
Dirty = 0
```

说明主存里已有正确副本，可以直接丢弃这一行。

如果替换时：

```
Dirty = 1
```

说明这一行有新数据，必须先写回主存，再替换。

## Cache、Block大小和缺失率的关系

Cache 性能与缺失率密切相关，而缺失率受 Cache 大小和 Block 大小影响。

1. Cache 大小的影响：
Cache 越大，可以保存的主存块越多，数据被替换出去的概率越小，因此缺失率一般越低。
但 Cache 越大，硬件成本、面积和功耗也越高。

2. Block 大小的影响：
Block 是主存和 Cache 之间传送数据的单位。
适当增大 Block 可以把更多相邻数据调入 Cache，利用空间局部性，降低缺失率。

但 Block 不能太大。
在 Cache 容量固定时，Block 越大，Cache 行数越少，能够同时保存的块数越少，容易产生替换和冲突，缺失率反而可能升高。

因此：
Block 太小，空间局部性利用不足；
Block 太大，Cache 行数减少、替换频繁；
Block 大小应结合 Cache 大小选择适中值。

## 为什么使用虚拟存储

![img](C:\Users\22067\Desktop\计组复习\assets\cover711_20260603100058.jpg)

### 1. 如果没有虚拟存储，会有什么问题？

PPT 说：

```
如果不用虚拟存储，程序员需要自己管理主存
```

这会带来两个麻烦。

#### 第一，不同计算机主存容量不同

比如你写一个程序，在一台电脑上主存很大：

```
内存 16GB
```

但另一台电脑主存很小：

```
内存 4GB
```

如果程序员要自己管理真实内存，就要考虑：

```
我的程序到底能用多少内存？
数据应该放在哪个真实地址？
这个地址在另一台电脑上还能不能用？
```

这就很麻烦。

------

#### 第二，多道程序共享主存

现代计算机不是一次只运行一个程序，而是多个程序同时运行。

比如：

```
微信
浏览器
游戏
编译器
操作系统
```

它们都要用主存。

如果没有虚拟存储，就要程序员自己考虑：

```
我的程序不能占用别人的内存
别人的程序也不能改我的内存
多个程序之间怎么分配空间
```

这显然不现实。

所以虚拟存储要解决的核心问题就是：

> **把复杂的主存管理交给操作系统和硬件，程序员只需要面对一个“好像自己独占”的地址空间。**

------

### 2. 虚拟存储的解决思路

PPT 写：

```
将地址空间和主存容量的概念区分开来
```

这句话很重要。

意思是：程序看到的地址空间，不一定等于真实主存大小。

程序运行时看到的是一个“虚拟的地址空间”。

比如程序以为自己可以使用：

```
0x00000000 ~ 0xFFFFFFFF
```

但这些地址不一定都真的在物理内存里。

真正运行时，由操作系统和硬件把虚拟地址转换成物理地址。

也就是：

```
虚拟地址 → 地址转换机制 → 物理地址
```

这个转换过程通常由：

```
操作系统 + MMU
```

共同完成。

MMU 是 Memory Management Unit，也就是内存管理单元。

------

### 3. 什么是逻辑地址？

PPT 写：

```
逻辑地址：程序中指令所用地址，称为虚拟地址
```

也就是说，程序代码里看到的地址，并不是真实主存地址，而是逻辑地址，也叫虚拟地址。

例如程序中有一条指令：

```
load R1, 1000
```

这里的 `1000` 不是一定表示主存中真实的第 1000 个单元。

它只是程序自己地址空间里的地址。

所以：

```
逻辑地址 = 虚拟地址 = 程序看到的地址
```

------

### 4. 什么是物理地址？

PPT 写：

```
物理地址：指令或数据的实际内存地址，也称为实地址、主存地址
```

也就是说，物理地址是真实主存里的位置。

比如经过地址转换以后：

```
虚拟地址 1000 → 物理地址 0x3A20
```

那么 CPU 最终真正访问的是：

```
主存 0x3A20 这个位置
```

所以：

```
物理地址 = 实地址 = 主存地址 = 真实内存中的地址
```

------

### 5. 用一个生活例子理解

可以把虚拟地址和物理地址理解成“快递地址”和“仓库货架”。

程序员看到的是虚拟地址：

```
我的东西放在第 1000 号位置
```

但真实内存里可能放在：

```
主存第 0x3A20 号位置
```

程序员不需要知道真实货架在哪，操作系统和硬件负责映射。

所以程序员只管说：

```
我要访问虚拟地址 1000
```

系统负责找到：

```
它实际在物理地址 0x3A20
```

------

### 6. 为什么这对多程序运行很重要？

假设有两个程序：

```
程序 A 访问虚拟地址 1000
程序 B 也访问虚拟地址 1000
```

它们看到的虚拟地址可以一样，但映射到的物理地址不同：

```
程序 A 的虚拟地址 1000 → 物理地址 0x2000
程序 B 的虚拟地址 1000 → 物理地址 0x8000
```

这样两个程序就不会互相干扰。

这就是虚拟存储的一个重要作用：

> **让每个程序都好像拥有自己独立的内存空间。**

![img](C:\Users\22067\Desktop\计组复习\assets\cover800_20260603100058.jpg)

### 3. 虚拟存储怎么实现？类似 Cache

PPT 写：

```
实现方法（类似 Cache）
```

为什么说类似 Cache？

因为 Cache 也是：

```
主存中一部分内容放到 Cache 中
需要时调入，不需要时替换
```

虚拟存储也是类似思想：

```
外存中的一部分内容放到主存中
需要时调入，不需要时留在外存
```

可以这样类比：

| 层次       | 小而快 | 大而慢    | 调入单位 |
| ---------- | ------ | --------- | -------- |
| Cache 系统 | Cache  | 主存      | 块 Block |
| 虚拟存储   | 主存   | 外存/磁盘 | 页 Page  |

所以虚拟存储可以理解成：

> **把主存当成外存的高速缓存。**

------

### 4. 内存被分成固定长度的存储块

PPT 写：

```
内存被分成固定长的存储块
```

这些固定大小的块叫：

```
页框 / 实页 / 物理页
```

它们都是指 **物理内存中的固定大小区域**。

比如物理内存可以被分成很多个 4KB 的页框：

```
页框 0
页框 1
页框 2
页框 3
...
```

每个页框大小相同，比如 4KB。

------

### 5. 每个进程也被分成固定长度的程序块

PPT 写：

```
每个进程也被划分成固定长的程序块
```

这些程序块叫：

```
虚页 / 逻辑页
```

也就是虚拟地址空间中的页。

比如一个程序的虚拟地址空间可以分成：

```
虚页 0
虚页 1
虚页 2
虚页 3
...
```

每个虚页大小也一样，比如 4KB。

------

### 6. 虚页可以装入页框中

PPT 写：

```
程序块可装到存储块中
```

意思是：

```
虚页 可以装入 物理页框
```

比如：

```
进程 A 的虚页 0 → 物理页框 5
进程 A 的虚页 1 → 物理页框 9
进程 A 的虚页 2 → 物理页框 2
```

重点是：**虚拟地址看起来连续，但实际装入物理内存时不需要连续。**

所以 PPT 写：

```
存放一个进程无需连续页框
```

这句话很关键。

也就是说，程序在虚拟地址空间中可能是连续的，但它对应的物理内存可以分散在不同地方。

------

### 7. 页表是什么？

PPT 写：

```
操作系统为每个进程生成一个页表；
用页表实现逻辑地址向物理地址转换。
```

页表的作用就是记录：

```
某个虚页现在放在哪个物理页框里
```

例如：

```
虚页号 0 → 页框号 5
虚页号 1 → 页框号 9
虚页号 2 → 页框号 2
```

当程序访问一个虚拟地址时，系统会先查页表，把虚拟地址转换成物理地址。

大致过程是：

```
虚拟地址
= 虚页号 + 页内偏移
```

然后查页表：

```
虚页号 → 页框号
```

最后形成物理地址：

```
物理地址 = 页框号 + 页内偏移
```

页内偏移不变，只是虚页号被翻译成页框号。

------

### 8. 一个进程必须全部装入内存吗？

PPT 最下面问：

```
一个进程必须全部都装入内存？
```

答案是：

```
不必须。
```

根据局部性原理，只需要把当前活跃的页面调入主存，其余页面可以留在磁盘上。

比如一个程序有 100 个虚页，但当前只用到第 3、4、5 页，那么可以只把这些页装入内存：

```
虚页 3 → 主存
虚页 4 → 主存
虚页 5 → 主存
其他虚页 → 外存/磁盘
```

等程序以后访问其他虚页时，再把需要的页调入主存。

这就是 **按需调页** 的思想。

## 虚拟存储技术的实质

![image-20260628143015203](C:\Users\22067\Desktop\计组复习\assets\image-20260628143015203.png)

![img](C:\Users\22067\Desktop\计组复习\assets\cover811_20260603100059.jpg)

### 一、虚拟存储到底解决什么问题？

PPT 第一页最核心的一句话是：

> **虚拟存储技术解决了主存容量与程序需求之间的矛盾。**

也就是说，程序想要的内存空间很大，但是实际主存容量有限。

比如你打开一个大型软件，它可能需要很多代码、数据、库文件。如果要求这些东西全部一次性装入主存，那主存压力会很大。而且多个程序同时运行时，每个程序都要占用内存，这个矛盾就更明显。

所以虚拟存储要解决的问题就是：

```
程序需要很大的存储空间
但真实主存容量有限
```

虚拟存储的办法是：

```
不要求整个程序全部装入主存
只把当前正在使用的部分放进主存
其他暂时不用的部分放在磁盘上
```

这样程序员感觉自己有很大的内存空间，但实际上主存里只放了一部分内容。

------

### 二、虚拟存储为什么需要操作系统和硬件一起实现？

PPT 上说：

> 虚拟存储器机制由硬件与操作系统协作实现。

这句话很重要。

因为虚拟存储不是单纯靠软件，也不是单纯靠硬件。

它需要两部分配合：

#### 1. 硬件负责地址转换

CPU 执行指令时，程序给出的地址是 **逻辑地址/虚拟地址**。

但是主存真正能识别的是 **物理地址**。

所以执行时必须把：

```
虚拟地址 → 物理地址
```

这个转换过程主要靠硬件完成，尤其是 MMU，也就是内存管理单元。

硬件负责让这个转换尽可能快。

------

#### 2. 操作系统负责内存管理

操作系统负责：

```
给每个进程分配虚拟地址空间
建立页表
管理哪些页面在主存
管理哪些页面在磁盘
处理缺页
进行进程切换
```

所以虚拟存储涉及很多操作系统概念，比如 PPT 上写的：

```
进程
进程上下文切换
存储器分配
虚拟地址空间
缺页处理
```

这也是为什么虚拟存储这一块既属于计组，也和操作系统联系很紧。

------

### 三、虚拟存储技术的实质是什么？

PPT 第一页下面写了“虚拟存储技术的实质”。

它可以拆成四句话理解。

------

#### 1. 程序员在逻辑地址空间中编写程序

程序员写程序时，用的是虚拟地址。

也就是说，程序里看到的地址并不是真实主存地址。

例如程序访问：

```
地址 1000
```

这个 `1000` 不是主存中真正的 1000 号单元，而是这个程序自己的虚拟地址空间中的 1000 号位置。

所以程序员不用关心：

```
真实内存有多大
我的程序实际放在哪个物理位置
其他程序占用了哪些内存
```

这些都交给操作系统和硬件处理。

------

#### 2. 程序执行时，一部分数据被调入主存，其他部分放在磁盘上

程序运行时，并不是整个程序都必须在主存中。

比如一个程序有 100 个页面，但当前只用到第 3 页、第 4 页、第 8 页，那么只需要把这些页面放进主存。

其他暂时不用的页面可以放在磁盘上。

这就利用了程序的局部性原理：

> 程序在一段时间内，通常只集中访问一小部分代码和数据。

所以虚拟存储不是让主存真的变大了，而是让主存和磁盘配合工作。

主存中放当前活跃部分，磁盘中放暂时不用部分。

------

#### 3. 指令执行时，通过硬件把逻辑地址转换为物理地址

程序执行时，CPU 发出的地址是虚拟地址。

但是最终访问主存必须用物理地址。

所以每次访存时，需要做地址转换：

```
虚拟地址 → 物理地址
```

这个转换一般通过页表完成。

虚拟地址可以拆成：

```
虚页号 + 页内地址
```

页表负责把：

```
虚页号 → 物理页框号
```

然后物理地址就是：

```
物理页框号 + 页内地址
```

注意，页内地址不变，只是前面的虚页号被换成了物理页框号。

------

#### 4. 缺页时，由操作系统控制主存和磁盘之间的数据交换

如果程序访问的页面已经在主存中，那就直接访问。

但如果访问的页面不在主存中，就会发生：

```
缺页
```

缺页就是：

> 程序要访问的虚拟页面当前没有装入主存。

这时候硬件会发现页表中该页“不在主存”，然后产生缺页异常。

操作系统接管后，会做这些事：

```
1. 暂停当前程序
2. 到磁盘中找到需要的页面
3. 在主存中找一个空闲页框
4. 把磁盘上的页面调入主存
5. 修改页表
6. 重新执行刚才那条访问指令
```

所以 PPT 说：

> 缺页时由操作系统控制主存和磁盘之间的数据交换。

------

### 四、第二页图在讲什么？

第二页标题是：

```
虚拟存储技术的实质
```

图里的核心是：

> **每个进程有自己的虚拟空间，每个进程有自己的页表，页表把虚拟空间映射到物理空间。**

我们看图里面几个部分。

------

#### 1. 左边：每个用户程序都有自己的虚拟空间

图左边有：

```
用户程序 1
用户程序 k
```

它们各自都有一个长条形的虚拟空间。

这表示：

> 每个进程都认为自己拥有一块独立、连续的地址空间。

比如程序 1 可能认为自己有：

```
虚拟地址 0 ~ 虚拟地址很大
```

用户程序 k 也认为自己有：

```
虚拟地址 0 ~ 虚拟地址很大
```

注意，它们可以都有相同的虚拟地址，比如都可以访问虚拟地址 `1000`。

但是它们对应的物理地址不一样。

这就是虚拟存储的重要作用：

```
让每个程序拥有独立的虚拟地址空间
```

------

#### 2. 中间：页表

图中间有：

```
页表1
页表k
```

PPT 上方写：

> 每个进程一个页表；页表将虚拟空间映射到物理空间。

意思是，每个进程都有自己的页表。

比如用户程序 1 有页表 1。

用户程序 k 有页表 k。

页表记录的是：

```
这个进程的虚拟页放在主存的哪个物理页框中
```

举个简单例子：

用户程序 1 的页表可能是：

```
虚页 0 → 物理页框 5
虚页 1 → 物理页框 9
虚页 2 → 不在主存，在磁盘
```

用户程序 k 的页表可能是：

```
虚页 0 → 物理页框 12
虚页 1 → 物理页框 3
虚页 2 → 不在主存，在磁盘
```

你看，两个程序都可以有虚页 0，但它们映射到的物理页框不同。

所以程序之间不会互相干扰。

------

#### 3. 右边：主存物理空间

图右边是：

```
主存物理空间
```

里面有：

```
操作系统
用户程序1片段
用户程序2片段
……
用户程序k片段
```

注意，主存中放的不是完整程序，而是程序的一部分页面。

所以图里写：

```
仅装入当前所需的代码和数据
```

这说明：

> 程序不需要全部装进主存，只需要把当前要用的页面放进去。

比如用户程序 k 可能有很多页面，但当前只需要执行其中一段代码，所以主存里只放用户程序 k 的某些片段。

------

#### 4. 下方：磁盘物理空间

图下面是：

```
磁盘物理空间
```

里面放着：

```
用户程序 k
用户程序 1
用户程序 2
```

磁盘中保存的是程序的完整内容，或者暂时没有装入主存的页面。

当主存中缺少某个页面时，操作系统会从磁盘把它调入主存。

这就是图中右边那个向上的箭头：

```
缺页时调入新页
```

意思是：

> 如果程序访问了不在主存中的页面，就从磁盘把这个页面调入主存。

------

### 五、虚拟存储和 Cache 很像，但层次不同

你可以把虚拟存储和 Cache 做一个类比。

Cache 是：

```
CPU 需要的数据如果不在 Cache，就从主存调入 Cache
```

虚拟存储是：

```
程序需要的页面如果不在主存，就从磁盘调入主存
```

对应关系是：

```
Cache系统：Cache ← 主存
虚拟存储：主存 ← 磁盘
```

Cache 的调入单位叫：

```
块 Block
```

虚拟存储的调入单位叫：

```
页 Page
```

所以它们思想相似：

```
小而快的存储器保存当前常用内容
大而慢的存储器保存完整内容
需要时再调入
```

区别是：

```
Cache主要由硬件自动完成
虚拟存储需要硬件和操作系统共同完成
```

------

### 六、虚拟地址到物理地址怎么转换？

这个地方是核心，我用一个小例子讲。

假设页面大小是 4KB。

因为：

```
4KB = 2^12B
```

所以页内地址需要 12 位。

一个虚拟地址可以拆成：

```
虚页号 + 页内地址
```

比如某个虚拟地址属于：

```
虚页号 = 5
页内地址 = 100
```

系统查页表发现：

```
虚页 5 → 物理页框 20
```

那么物理地址就是：

```
物理页框 20 + 页内地址 100
```

也就是说，地址转换只改变页号部分：

```
虚页号 5 变成 物理页框号 20
页内地址 100 不变
```

所以：

```
虚拟地址 = 虚页号 + 页内偏移
物理地址 = 物理页框号 + 页内偏移
```

这个公式非常重要。

------

### 七、如果页面不在主存怎么办？

如果查页表时发现：

```
虚页 5 不在主存
```

就会发生缺页。

缺页处理过程是：

```
1. CPU 访问某个虚拟地址
2. 查页表发现该页不在主存
3. 产生缺页异常
4. 操作系统暂停当前程序
5. 从磁盘找到这个页面
6. 把页面调入主存某个空闲页框
7. 修改页表
8. 重新执行刚才那条指令
```

所以程序员写程序的时候不用自己写“从磁盘调页”的代码，这些都是操作系统自动处理的。

------

### 八、为什么一个进程不需要连续存放？

以前如果没有分页，一个程序可能要连续占一大块物理内存。

比如程序需要 100KB，就要找一段连续的 100KB 主存空间。

但分页以后，程序被分成很多页。

每一页可以放到不同的物理页框中。

比如：

```
虚页 0 → 页框 8
虚页 1 → 页框 2
虚页 2 → 页框 15
虚页 3 → 页框 6
```

它们在物理内存中可以不连续。

但是程序自己看到的虚拟地址空间仍然是连续的。

所以虚拟存储带来的一个好处是：

```
程序逻辑上连续
物理上可以分散存放
```

这大大方便了内存分配。

------

### 九、虚拟存储带来的几个好处

这两页 PPT 实际上想让你理解几个好处。

#### 1. 程序员不用管理主存

程序员只面对虚拟地址空间，不需要关心真实内存分配。

#### 2. 每个程序好像独占内存

每个进程都有自己的页表和虚拟地址空间，因此互不干扰。

#### 3. 程序可以大于实际主存

只要不是所有页面同时使用，就可以把暂时不用的页面放在磁盘上。

#### 4. 提高主存利用率

主存只放当前活跃页面，避免把暂时不用的代码和数据占在主存里。

#### 5. 支持多道程序运行

多个程序可以共享主存，每个程序只装入当前需要的页面

## 虚拟存储器管理

![img](C:\Users\22067\Desktop\计组复习\assets\cover602_20260603100059.jpg)

## “主存——磁盘”层次

![img](C:\Users\22067\Desktop\计组复习\assets\cover603_20260603100059.jpg)

“主存—磁盘”层次是虚拟存储研究的主要层次。
可以把主存看作磁盘的高速缓存。

1. 页大小比 Cache 块大
   Cache 块通常较小，而虚拟存储的页一般为 2KB~64KB。
   因为磁盘访问代价很大，所以一次调入较大的页面可以减少频繁访问磁盘。

2. 采用全相联映射
   每个虚拟页可以放入任意物理页框。
   这样可以提高主存利用率，减少冲突，降低缺页率。

3. 缺页由软件处理
   缺页涉及磁盘访问、页面调入调出、页表修改和进程恢复等复杂操作，
   因此由操作系统处理，而不是完全靠硬件完成。

4. 采用 Write Back 写回策略
   主存中的页面被修改后，不立即写回磁盘，
   而是在页面被换出时，如果修改位为 1，再写回磁盘。
   这样可以减少磁盘访问次数。

5. 地址转换由硬件实现
   程序每次访存都要进行虚拟地址到物理地址的转换，
   为了加快执行速度，地址转换由 MMU、TLB 等硬件完成。

## 用户程序的页表以及主存中的页表示例

![img](C:\Users\22067\Desktop\计组复习\assets\cover605_20260603100059.jpg)

![img](C:\Users\22067\Desktop\计组复习\assets\cover606_20260603100101.jpg)

页表是虚拟存储中实现地址转换的核心结构。
每个进程有自己的页表，页表存放在主存中，其首地址保存在页表基址寄存器中。

页表的每一行称为页表项，对应一个虚页。
页表项中通常记录：
1. 装入位/有效位：表示该页是否在主存中。
2. 实页号/物理页框号：表示该虚页在主存中的位置。
3. 修改位/脏位：表示该页是否被修改过，换出时是否需要写回磁盘。
4. 替换控制位：用于页面替换算法，如 FIFO、LRU 等。
5. 访问权限位：控制该页是否可读、可写、可执行。
6. 外存地址：当该页不在主存时，指出其在磁盘中的位置。

页表项有三种状态：
1. 已分配的装入页：
   该虚页属于进程，且已经在主存中，页表项中装入位为 1，并记录物理页框号。

2. 已分配的未装入页：
   该虚页属于进程，但当前不在主存，而在磁盘中。访问时会发生缺页，需要由操作系统调入主存。

3. 未分配页：
   该虚页尚未对应任何主存或磁盘空间，页表项为空 null。非法访问会产生异常。

页表行数 = 虚拟地址空间大小 / 页面大小。

## 逻辑地址转换为物理地址的过程

![img](C:\Users\22067\Desktop\计组复习\assets\cover607_20260603100101.jpg)

### 1. 先看虚拟地址被分成两部分

图最上面是：

```
虚地址 = 虚拟页号 + 页内偏移
```

也就是说，程序发出的逻辑地址不是一个整体直接用，而是拆开看。

比如：

```
虚拟页号：说明这个地址属于程序的第几页
页内偏移：说明它是这一页里面的第几个字节/单元
```

图里页内偏移上面标了：

```
10
```

说明页内偏移占 10 位。

所以页面大小是：

```
2^10 = 1024B = 1KB
```

也就是说，每一页大小是 1KB。

------

### 2. 为什么页内偏移不用转换？

这是这页最重要的点之一。

虚拟地址：

```
虚拟页号 + 页内偏移
```

转换成物理地址：

```
物理页号 + 页内偏移
```

你会发现：

```
页号变了，但页内偏移不变。
```

为什么？

因为虚页和物理页框大小一样。

比如页面大小都是 1KB，那么虚页里的第 100 个字节，装到物理页框后，仍然是那个页框里的第 100 个字节。

所以地址转换只需要把：

```
虚拟页号 → 物理页号
```

而页内偏移直接保留。

------

### 3. 页表基址寄存器是干什么的？

图左边有：

```
页表基址寄存器
```

它里面存的是当前进程页表的首地址。

因为页表本身存放在主存中，所以 CPU 要查页表时，必须先知道页表从哪里开始。

可以理解成：

```
页表基址寄存器 = 当前进程页表的起点
```

然后虚拟页号作为索引，找到对应页表项。

真正查表地址可以理解为：

```
页表项地址 = 页表首地址 + 虚拟页号 × 每个页表项大小
```

也就是说：

```
页表基址寄存器告诉你页表在哪；
虚拟页号告诉你查页表的第几项。
```

------

### 4. 页表项里面有什么？

图中页表项里有：

```
V
Access Rights
物理页号
```

这三个是核心。

------

#### 1）V 位：装入位 / 有效位

图里写：

```
V=1时，已装入
```

意思是：

```
V = 1：这个虚拟页已经在主存中
V = 0：这个虚拟页不在主存中
```

如果 V=1，就可以继续地址转换。

如果 V=0，说明这个页面还在磁盘上，没有调入主存，就会发生：

```
缺页异常
```

------

#### 2）Access Rights：访问权限位

这个字段表示当前程序是否有权限访问这个页面。

比如：

```
是否可读
是否可写
是否可执行
用户态是否能访问
```

如果程序访问方式不合法，就会发生保护异常。

比如某个页是只读的，但程序想写它：

```
权限不允许 → 保护违例异常
```

所以图右下角写：

```
异常：缺页和保护违例
```

这里的异常主要有两类：

```
V=0 → 缺页异常
权限不符合 → 保护违例异常
```

------

#### 3）物理页号

如果 V=1，并且权限检查通过，那么页表项中的物理页号就会被取出来。

它表示：

```
这个虚拟页当前放在主存的哪个物理页框中
```

比如：

```
虚拟页 3 → 物理页 12
```

那么物理页号就是 12。

------

### 5. 地址转换完整过程

可以按照这个顺序理解：

------

#### 第一步：CPU 给出虚拟地址

程序执行时，CPU 发出虚拟地址：

```
虚拟地址 = 虚拟页号 + 页内偏移
```

------

#### 第二步：用虚拟页号查页表

页表基址寄存器给出页表首地址。

虚拟页号作为索引，找到对应页表项。

```
页表基址 + 虚拟页号索引 → 对应页表项
```

------

#### 第三步：检查 V 位

如果：

```
V = 1
```

说明该页已经在主存中，可以继续。

如果：

```
V = 0
```

说明该页不在主存中，发生缺页异常，由操作系统从磁盘调页。

------

#### 第四步：检查访问权限

如果访问权限允许，就继续转换。

如果权限不允许，就发生保护异常。

比如：

```
页面只读，但程序要写 → 保护违例
```

------

#### 第五步：得到物理页号

从页表项中取出：

```
物理页号
```

------

#### 第六步：拼接形成物理地址

最后把：

```
物理页号 + 页内偏移
```

拼成物理地址。

注意，页内偏移直接来自原来的虚拟地址，不需要改变。

------

### 6. 举个简单例子

假设页面大小是 1KB。

因为：

```
1KB = 2^10B
```

所以页内偏移占 10 位。

假设 CPU 发出的虚拟地址被拆成：

```
虚拟页号 = 5
页内偏移 = 100
```

然后查页表：

```
虚页 5 → 物理页号 12
V = 1
访问权限允许
```

那么最终物理地址就是：

```
物理页号 12 + 页内偏移 100
```

也就是：

```
主存第 12 号物理页框中的第 100 个字节
```

所以转换过程就是：

```
虚拟地址：虚页 5 的第 100 个字节
↓ 查页表
物理地址：物理页 12 的第 100 个字节
```

------

### 7. 如果 V=0 会怎么样？

如果查页表发现：

```
V = 0
```

说明这个虚页当前不在主存里。

这时不能直接形成物理地址，而是发生缺页。

缺页处理过程大概是：

```
1. CPU 发现该页不在主存
2. 产生缺页异常
3. 操作系统接管
4. 从磁盘找到该页
5. 把它调入主存的某个物理页框
6. 修改页表项，让 V=1，并填入物理页号
7. 重新执行刚才的指令
```

所以缺页不是程序员自己处理，而是操作系统处理。

------

### 8. 如果权限不对会怎么样？

如果 V=1，说明页面在主存中，但还要检查权限。

比如页表项规定：

```
该页只读
```

但是程序执行写操作：

```
store 到这个页
```

这时就会发生：

```
保护违例异常
```

也就是程序访问了不该访问的内容，或者用不允许的方式访问。

这就是虚拟存储中的保护机制。

## TLBs

![img](C:\Users\22067\Desktop\计组复习\assets\cover609_20260603100102.jpg)

![img](C:\Users\22067\Desktop\计组复习\assets\cover610_20260603100102.jpg)

### 一、为什么需要 TLB？

前面我们讲过，虚拟地址转换成物理地址，需要查页表。

普通流程是：

```
CPU 给出虚拟地址
↓
用虚拟页号查页表
↓
得到物理页框号
↓
拼接页内偏移
↓
访问主存中的数据
```

问题来了：

> **页表本身存在哪里？**

页表存在主存里。

所以如果没有 TLB，一次访存至少要访问主存两次：

```
第 1 次：访问主存中的页表，完成地址转换
第 2 次：访问主存中的真正数据
```

所以 PPT 第一页问：

```
如前所述流程一次存储器引用至少要访问几次主存？
```

答案是：

```
至少 2 次主存访问
```

这就很慢。因为本来 CPU 只是想取一个数据，结果还要先查页表，多访问一次主存。

------

### 二、TLB 是什么？

PPT 上写：

```
把最活跃的页表项放到 Cache 中，称为 Translation Lookaside Buffer，简称 TLB
```

意思是：

> **TLB 是专门用来缓存最近使用过的页表项的小型高速存储器。**

页表很大，存在主存中；TLB 很小，但速度很快，通常在 CPU 内部。

可以这样理解：

```
页表：完整通讯录，但在主存里，查起来慢
TLB：最近联系人列表，很小，但查起来快
```

因为程序有局部性，最近访问过的虚页很可能马上还会访问，所以把这些最近用过的页表项放进 TLB，可以大幅减少查页表次数。

------

### 三、TLB 里面存什么？

第一页图中 TLB 表项包括：

```
Virtual Address(tag)
Physical Address
Dirty
Ref
Valid
Access
```

也就是：

```
虚拟页号标记 + 物理页框号 + 一些状态位
```

注意，这里的 `Virtual Address(tag)` 更准确地说是：

```
虚拟页号 VPN 的标记部分
```

不是完整虚拟地址。因为页内偏移不需要转换，所以 TLB 主要保存：

```
虚拟页号 → 物理页框号
```

------

#### 1. Virtual Address(tag)

这个字段用于判断：

```
当前 CPU 要访问的虚拟页，是不是这个 TLB 表项记录的虚拟页
```

比如 CPU 要访问虚页 5，TLB 中某项 tag 也是虚页 5，那么可能命中。

------

#### 2. Physical Address / page frame

这个字段保存对应的物理页框号。

比如：

```
虚拟页 5 → 物理页框 12
```

那么 TLB 中就会保存物理页框号 12。

地址转换时，最终物理地址是：

```
物理页框号 + 页内偏移
```

------

#### 3. Valid 有效位

Valid 表示这个 TLB 项是否有效。

```
Valid = 1：这个 TLB 项有效
Valid = 0：这个 TLB 项无效
```

如果 Valid = 0，即使 tag 看起来相同，也不能用。

------

#### 4. Dirty 修改位

Dirty 表示这一页是否被写过。

```
Dirty = 1：该页被修改过
Dirty = 0：该页没有被修改过
```

如果该页将来被换出主存，Dirty = 1 时需要写回磁盘。

------

#### 5. Ref 引用位

Ref 也可以叫访问位。

它表示这个页最近是否被访问过，常用于页面替换算法。

比如 Clock、LRU 近似算法会用到这个信息。

------

#### 6. Access 权限位

Access 表示访问权限，比如：

```
可读
可写
可执行
用户态是否能访问
```

如果程序没有权限访问，就会触发保护异常。

------

### 四、TLB 怎么查？

第二页图中说：

```
先由虚页号到 TLB 中找
```

也就是 CPU 发出虚拟地址后，先拆成：

```
虚拟地址 = 虚拟页号 + 页内偏移
```

然后拿虚拟页号去 TLB 查。

------

#### 情况一：TLB 命中

如果 TLB 中某一项满足：

```
Valid = 1
并且 tag = 当前虚拟页号
```

那么说明 TLB 命中。

这时可以直接得到物理页框号。

然后：

```
物理地址 = 物理页框号 + 页内偏移
```

最后访问主存中的数据。

所以 TLB 命中时，主存访问次数通常是：

```
1 次
```

因为不用访问主存中的页表，只访问真正的数据。

------

#### 情况二：TLB 未命中

如果：

```
TLB 中没有匹配的 tag
或者匹配项 Valid = 0
```

那就是 TLB 未命中。

这时候怎么办？

去主存中的页表查。

流程变成：

```
1. TLB 未命中
2. 去主存页表查对应页表项
3. 如果页表项有效，得到物理页框号
4. 把这个页表项装入 TLB
5. 再用物理地址访问主存数据
```

所以 TLB 未命中但页表命中时，还是要访问页表。

------

#### 情况三：页表中也无效

如果去页表查，发现页表项的 V 位为 0，说明：

```
这个虚页当前不在主存
```

这时发生：

```
缺页异常
```

操作系统要从磁盘把这个页面调入主存，然后更新页表，必要时也更新 TLB。

------

### 五、为什么第二页说 TLB 是全相联？

图中右上角写：

```
这里 TLB 映射方式？
全相联！VP# 需和每个 tag 比较
```

意思是：

> TLB 中每个表项都可能保存任意虚拟页的映射，所以查找时，要把当前虚拟页号和 TLB 中所有项的 tag 同时比较。

这和 Cache 的全相联类似。

比如 TLB 有 8 项，CPU 要访问虚页 5。

它会同时比较：

```
虚页 5 是否等于第 0 项 tag
虚页 5 是否等于第 1 项 tag
虚页 5 是否等于第 2 项 tag
……
虚页 5 是否等于第 7 项 tag
```

如果某一项匹配，并且 Valid = 1，就命中。

为什么 TLB 可以这样做？

因为 TLB 很小。

页表可能有成千上万项，但 TLB 可能只有几十项或几百项，所以可以用高速硬件并行比较。

------

### 六、引入 TLB 的目的是什么？

第二页下面问：

```
引入 TLB 的目的是什么？
```

答案就是：

> **减少访问主存中页表的次数，加快虚拟地址到物理地址的转换。**

不用 TLB：

```
查页表一次 + 访问数据一次
= 至少 2 次主存访问
```

有 TLB 且命中：

```
查 TLB 很快
直接访问数据
= 只需要 1 次主存访问
```

所以 TLB 能显著提高地址转换速度。

------

### 七、TLB 和页表的关系

可以这样理解：

```
页表：完整的虚页到物理页框的映射表，存在主存中，容量大但慢。
TLB：页表中最近常用项的副本，存在 CPU 内部，容量小但快。
```

TLB 不是替代页表，而是加速页表。

如果 TLB 命中，就不用查页表。

如果 TLB 未命中，就还是要查页表。

------

### 八、完整访问流程

CPU 访问某个虚拟地址时：

```
1. CPU 产生虚拟地址
2. 将虚拟地址拆成：虚拟页号 + 页内偏移
3. 用虚拟页号查 TLB
```

如果 TLB 命中：

```
4. 取出物理页框号
5. 拼接页内偏移形成物理地址
6. 访问主存数据
```

如果 TLB 未命中：

```
4. 去主存页表中查对应页表项
5. 如果页表项有效，得到物理页框号
6. 将该映射填入 TLB
7. 拼接物理地址
8. 访问主存数据
```

如果页表项无效：

```
发生缺页异常
由操作系统从磁盘调页
更新页表和 TLB
重新执行访问
```

------

### 九、用一个例子理解

假设 CPU 要访问虚拟地址，拆开后得到：

```
虚拟页号 = 8
页内偏移 = 100
```

CPU 先查 TLB。

#### 如果 TLB 中有：

```
tag = 8
Valid = 1
page frame # = 20
```

说明命中。

那么物理地址就是：

```
物理页框 20 + 页内偏移 100
```

CPU 直接访问主存中这个位置即可。

------

#### 如果 TLB 中没有 tag = 8

说明 TLB 未命中。

这时去主存页表查虚页 8。

如果页表中写着：

```
虚页 8 → 物理页框 20
V = 1
```

那就说明页面在主存中，只是 TLB 里暂时没有。

于是系统把这个映射放进 TLB：

```
tag = 8
page frame # = 20
Valid = 1
```

之后再访问虚页 8，就很可能 TLB 命中。

------

#### 如果页表中 V = 0

说明虚页 8 不在主存。

这时发生缺页，要从磁盘调入

## CPU访存过程图（重要重要重要⚠️）

![img](C:\Users\22067\Desktop\计组复习\assets\cover613_20260603100102.jpg)

![img](C:\Users\22067\Desktop\计组复习\assets\cover722_20260603100102.jpg)

## 分段式虚拟存储器

![img](C:\Users\22067\Desktop\计组复习\assets\cover614_20260603100103.jpg)

分段式虚拟存储器：

分页式虚拟存储虽然页面大小固定、实现简单，但页不是程序逻辑上的独立实体，一条指令或一个数据可能跨越两个页面。

分段式虚拟存储按照程序的逻辑结构划分地址空间，将程序划分为多个相对独立的段，如代码段、只读数据段、可读写数据段、堆段、栈段等。各段长度可以不同。

每个段通常具有段名、基地址和长度，便于编写程序、编译器优化以及操作系统管理。

分段系统中，每个进程都有自己的段表，段表由段表项组成。段表项中通常记录段基地址、段长、访问权限等信息。

逻辑地址格式：
段号 + 段内偏移

地址转换过程：
1. 根据段号查段表；
2. 取出段基地址和段长；
3. 判断段内偏移是否小于段长；
4. 若合法，则物理地址 = 段基地址 + 段内偏移；
5. 若越界或权限不符，则产生异常。

分段式优点：
符合程序逻辑结构，便于共享和保护。

分段式缺点：
段长不固定，容易产生外部碎片，内存管理较复杂。

## 段式虚拟存储器的地址映像

![img](C:\Users\22067\Desktop\计组复习\assets\cover615_20260603100103.jpg)

### 一、这张图整体在表达什么？

左边是 **程序空间**，也就是程序员看到的逻辑空间。

它被分成了 4 个段：

```
0 段
1 段
2 段
3 段
```

每个段都有自己的长度，而且每个段内部的地址都是从 0 开始算的。

比如图左边：

```
0 段：0 ~ 16K
1 段：0 ~ 28K
2 段：0 ~ 10K
3 段：0 ~ 36K
```

注意：这些段在程序看来是独立的逻辑模块。

但是到了右边的主存中，它们并不是按照 0、1、2、3 的顺序连续存放的，而是分散放在主存的不同位置。

比如图中：

```
2 段放在主存 8K 附近
0 段放在主存 24K 附近
1 段放在主存 48K 附近
3 段放在主存 80K 附近
```

所以分段式的一个特点是：

> **程序逻辑上按段组织，物理内存中各段可以分散存放，但每个段本身在主存中通常是连续的。**

------

### 二、中间的段表是干什么的？

中间这个表叫 **段表**。

PPT 下面写：

```
段表由段表项组成
```

段表里的每一行对应一个段。

比如：

```
第 0 行：记录 0 段的信息
第 1 行：记录 1 段的信息
第 2 行：记录 2 段的信息
第 3 行：记录 3 段的信息
```

表中主要有这些字段：

```
段名
装入位
段长
起始地址
访问方式
```

------

### 三、段表每一列是什么意思？

#### 1. 段名

段名就是段号。

比如：

```
0 段
1 段
2 段
3 段
```

CPU 给出逻辑地址时，里面会有一个段号。系统就根据段号去段表中找到对应的段表项。

------

#### 2. 装入位

装入位表示这个段当前是否已经在主存中。

```
装入位 = 1：该段已经装入主存
装入位 = 0：该段不在主存
```

图中几个段的装入位都是 1，说明这些段都已经在主存中。

如果某个段的装入位是 0，程序又访问了这个段，就会发生异常，叫：

```
缺段
```

这个和分页里的“缺页”很像。

分页中，页不在主存叫缺页；分段中，段不在主存叫缺段。

------

#### 3. 段长

段长表示这个段的大小。

比如表中：

```
0 段：16K
1 段：28K
2 段：10K
3 段：36K
```

段长用来检查地址是否越界。

比如访问 0 段，0 段长度是 16K，那么段内偏移必须小于 16K。

如果访问：

```
0 段，偏移 10K
```

合法。

如果访问：

```
0 段，偏移 20K
```

就不合法，因为 20K 超过了 0 段的长度 16K。

这就叫：

```
地址出界
```

------

#### 4. 起始地址

起始地址表示这个段在主存中的起始物理地址。

表中大概是：

```
0 段：起始地址 24K
1 段：起始地址 48K
2 段：起始地址 8K
3 段：起始地址 80K
```

所以如果访问某个段里的一个地址，就要用：

```
物理地址 = 段起始地址 + 段内偏移
```

------

#### 5. 访问方式

访问方式用来做保护检查。

比如某个段可以设置成：

```
只读
可读可写
可执行
不可写
```

例如代码段一般只允许读和执行，不允许写。

如果程序想写代码段，就会触发：

```
保护违例
```

------

### 四、地址转换过程

假设 CPU 给出一个逻辑地址：

```
段号 = 1
段内偏移 = 20K
```

那么系统这样转换：

#### 第一步：用段号查段表

段号是 1，所以查段表第 1 行。

得到：

```
1 段装入位 = 1
1 段段长 = 28K
1 段起始地址 = 48K
```

------

#### 第二步：检查装入位

装入位是 1，说明 1 段已经在主存中，可以继续。

如果装入位是 0，就发生缺段。

------

#### 第三步：检查是否越界

段内偏移是 20K。

1 段长度是 28K。

因为：

```
20K < 28K
```

所以没有越界。

------

#### 第四步：检查访问权限

如果访问方式允许，就继续。

如果访问方式不允许，比如该段只读但你要写，就发生保护违例。

------

#### 第五步：计算物理地址

用公式：

```
物理地址 = 段起始地址 + 段内偏移
```

代入：

```
物理地址 = 48K + 20K = 68K
```

所以这个逻辑地址最终访问的是主存中的：

```
68K 地址处
```

------

### 五、再举几个例子

#### 例 1：访问 0 段，偏移 4K

段表中：

```
0 段段长 = 16K
0 段起始地址 = 24K
```

检查：

```
4K < 16K
```

合法。

物理地址：

```
24K + 4K = 28K
```

所以访问主存 28K 位置。

------

#### 例 2：访问 2 段，偏移 8K

段表中：

```
2 段段长 = 10K
2 段起始地址 = 8K
```

检查：

```
8K < 10K
```

合法。

物理地址：

```
8K + 8K = 16K
```

所以访问主存 16K 位置。

------

#### 例 3：访问 2 段，偏移 12K

段表中：

```
2 段段长 = 10K
```

但偏移是 12K。

因为：

```
12K > 10K
```

所以这个地址越界。

这时不能计算物理地址，而是触发：

```
地址出界异常
```

------

### 六、这张图里的异常情况

PPT 下面列了三种异常：

```
缺段
地址出界
保护违例
```

这三个非常重要。

------

#### 1. 缺段

```
缺段：装入位 = 0
```

意思是程序访问的段不在主存中。

比如访问 3 段，但段表中 3 段装入位是 0，那么系统就知道：

```
3 段当前不在主存
```

这时要由操作系统把该段从磁盘调入主存。

这类似分页系统中的缺页。

------

#### 2. 地址出界

```
地址出界：偏移量超过最大段长
```

也就是说，程序访问了这个段范围之外的位置。

比如：

```
0 段长度 16K
程序访问 0 段偏移 20K
```

这就越界了。

段式系统天然容易做越界检查，因为段表里本来就记录了段长。

------

#### 3. 保护违例

```
保护违例：访问操作与访问方式不匹配
```

比如：

```
代码段只允许读和执行
但程序想写代码段
```

或者：

```
只读数据段不能写
但程序执行写操作
```

这就会触发保护违例。

------

### 七、分段式地址映射和分页式有什么不同？

分页式地址转换是：

```
虚拟地址 = 虚页号 + 页内偏移
物理地址 = 物理页号 + 页内偏移
```

分页中，页大小固定，只需要把虚页号换成物理页号。

分段式地址转换是：

```
逻辑地址 = 段号 + 段内偏移
物理地址 = 段起始地址 + 段内偏移
```

分段中，段大小不固定，所以必须检查段内偏移有没有超过段长。

所以分段比分页多了一个很重要的检查：

```
段内偏移 < 段长
```

------

### 八、这张图想强调什么？

它想强调三件事。

第一，程序空间是按逻辑段划分的：

```
0 段、1 段、2 段、3 段
```

第二，主存空间不必按照程序段的顺序连续存放：

```
2 段可以放前面
0 段可以放中间
1 段、3 段可以放后面
```

第三，段表负责完成地址映射和保护检查：

```
查装入位
查段长
查起始地址
查访问权限
```

## 示例：访问段号1偏移量13K的虚地址

![img](C:\Users\22067\Desktop\计组复习\assets\cover801_20260603100103.jpg)

分段式虚拟存储器的地址映射：

虚拟地址由段号和段内偏移组成。

地址转换过程：
1. 根据段号查段表；
2. 检查装入位，若装入位为 0，则发生缺段；
3. 检查段内偏移是否小于段长，若偏移量超过段长，则地址出界；
4. 检查访问方式是否匹配，若不匹配，则保护违例；
5. 若以上检查都通过，则：
   物理地址 = 段基地址 + 段内偏移。

例：
访问段号 1、偏移量 13K 的虚地址。
查段表得：
段 1 长度为 40K，基地址为 200K，装入位为 1。
因为 13K < 40K，所以没有越界；
装入位为 1，说明该段已经在主存中。
因此：
物理地址 = 200K + 13K = 213K。

## 存储保护的概念

![img](C:\Users\22067\Desktop\计组复习\assets\cover723_20260603100104.jpg)

存储保护是指系统限制每个进程只能访问属于自己或被授权的存储区域，防止程序非法访问其他程序、操作系统或重要数据。

存储区域可以设置不同权限：
1. 自己的存储区：可读可写、只读或只可执行；
2. 共享区：获得授权后可访问，通常可读不可写；
3. 未授权区域，如 OS 内核、页表等：不可访问。

发生存储保护错误的情况：
1. 地址越界：访问地址超出允许范围；
2. 访问越权：访问操作与权限不符，比如对只读页执行写操作。

存储保护通常通过页表或段表中的访问权限位实现。

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_计算机组成原理_课程MOC|计算机组成原理 MOC]]

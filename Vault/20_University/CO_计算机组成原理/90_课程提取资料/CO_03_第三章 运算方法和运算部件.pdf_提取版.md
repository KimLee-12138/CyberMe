---
id: extract-co-dfd6ee2a
type: extract
status: extracted
course: CO
source:
  - "[[91_Raw-Archive/PDF/CO_03_第三章 运算方法和运算部件.pdf_课件_未知日期_dfd6ee2a.pdf]]"
source_pages: all
source_hash: "dfd6ee2a84e7b88a31b288fcc4a444349cb5f6c02476fd13255504eb917e6078"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 03_第三章 运算方法和运算部件.pdf：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

## 第1页

运算方法和运算部件  
cover:小孩不哭站起来 lu
复习大纲  
1. 高级语言和机器指令中的运算及 ALU
2. 定点数运算及其运算部件
3. 浮点数运算及其运算部件
高级语言中涉及的运算（以 c 语言为例）  
1. 算术运算
1. 无符号数、带符号整数、浮点数的 +-*/ 运算
2. 按位运算
3. 逻辑运算
4. 移位运算
1. 无符号数
1. 左移：右侧直接补 0
2. 右移：左侧直接补 0
2. 有符号数
1. 左移：右侧补  0 ，低位补  0 ；高位移出，可能导致溢出或符号位改变
2. 右移：左侧补符号位
位拓展和位截断  
核心就两种情况：
1. 什么是 “ 位扩展 ” ？  
位扩展就是：原来的数据位数不够，现在要变成长的数据类型，所以要在前面补位。
比如：
如果  short 是 2 字节，int 是 4 字节，那么就是：
前面要多补  16 位。短类型 → 长类型：位扩展
长类型 → 短类型：位截断
short → int
16 位 → 32 位

## 第2页

无符号数扩展：前面补  0  
例如：
扩展成  32 位：
也就是：
这叫  0 扩展。
有符号数扩展：前面补符号位  
有符号数最高位是符号位：
如果是负数，最高位是  1，扩展时前面就补  1。
例如：
这是  16 位补码中的  -32768。
扩展成  32 位：
也就是：
这叫  符号扩展 。
2. 什么是 “ 位截断 ” ？  
位截断就是：原来的数据太长，现在要变成短的数据类型，所以直接丢掉高位，只保留低位。
比如：
如果  int 是 4 字节，short 是 2 字节：unsigned short: 1000 0000 0000 0000
0000 0000 0000 0000 1000 0000 0000 0000
00 00 80 00
0 表示正数
1 表示负数
short: 1000 0000 0000 0000
1111 1111 1111 1111 1000 0000 0000 0000
FF FF 80 00
int → short

## 第3页

就只保留低  16 位，高  16 位直接丢掉。
例如：
截断成  short ，只保留后  16 位：
这个  16 位补码表示的是：
所以数值可能发生变化
例 1：复习扩展  
题目：
假设：32 位 → 16 位
int: 0000 0000 0000 0000 1000 0000 0000 0000
1000 0000 0000 0000
-32768
short si = -32768;
unsigned short usi = si;
int i = si;
unsigned ui = usi;
short = 2 字节
int = 4 字节

## 第4页

1. short si = -32768;  
16 位补码中， -32768 表示为：
十六进制是：
所以：
2. unsigned short usi = si;  
这里是把  short 转成  unsigned short。
注意：二进制位不变，只是解释方式变了。
原来：
作为  signed short 看，是：
作为  unsigned short 看，是：
所以：
3. int i = si;  
这里是：
短变长，需要扩展。
因为  si 是有符号数，而且是负数，最高位是  1，所以做 符号扩展 ，前面补  1。1000 0000 0000 0000
80 00
si = -32768
存储内容： 80 00
1000 0000 0000 0000
-32768
32768
usi = 32768
存储内容： 80 00
short → int

## 第5页

变量 数值 存储内容
si -32768 80 00
usi 32768 80 00
i -32768 FF FF 80 00
ui 32768 00 00 80 00十六进制：
所以：
4. unsigned ui = usi;  
这里是：
无符号数短变长，做  0 扩展，前面补  0。
十六进制：
所以：
例 1 总结  原来 16 位：
1000 0000 0000 0000
扩展成 32 位：
1111 1111 1111 1111 1000 0000 0000 0000
FF FF 80 00
i = -32768
存储内容： FF FF 80 00
unsigned short → unsigned int
原来 16 位：
1000 0000 0000 0000
扩展成 32 位：
0000 0000 0000 0000 1000 0000 0000 0000
00 00 80 00
ui = 32768
存储内容： 00 00 80 00

## 第6页

重点：
例 2：截断  
题目：
问：
答案：
1. int i = 32768;  
32768 的  32 位二进制是：
十六进制：
所以：
2. short si = (short)i;  
这里是：
长变短，直接截断，只保留低  16 位。同样是 80 00 ，按  signed 看是  -32768 ，按  unsigned 看是  32768 。
int i = 32768;
short si = (short)i;
int j = si;
i 和 j 是否相等？
不相等
0000 0000 0000 0000 1000 0000 0000 0000
00 00 80 00
i = 32768
int → short
原来：
0000 0000 0000 0000 1000 0000 0000 0000
保留低 16 位：
1000 0000 0000 0000

## 第7页

也就是：
但是  short 是有符号数， 80 00 表示：
所以：
3. int j = si;  
这里又是：
短变长，且  si 是有符号负数，所以做符号扩展。
十六进制：
所以：
4. 最后比较  
所以：80 00
-32768
si = -32768
short → int
short:
1000 0000 0000 0000
扩展成 int:
1111 1111 1111 1111 1000 0000 0000 0000
FF FF 80 00
j = -32768
i = 32768
j = -32768
i != j

## 第8页

如何实现高级语言源程序中的运算我（ …… ）  
ALU  
ALU可进行加/减算术运算和 逻辑运算。其核心部件是 加法器。串行加法器在数字逻辑课学过 , 下面简单回
顾。
半加器  
半加器： 只考虑本位相加不考虑进位。
全加器  
全加器： 本位 A和 B 加上进位 Carryln 生成一位 Sum 和一位进位 CarryOut.
假定与 /或门延迟为 1T, 异或门 3T, 则 " 和 "Sum 与 " 进位 "Carryout 的延迟为多少 ?
Sum延迟两级异或门共 6T;Carryout 延迟一级与门和一级或门共 2T （因为与门是并行处理的）
串行进位加法器  
1. 基本功能  
串行进位加法器可以实现两个   位数逐位相加：
得到和：
进位输出为：

## 第9页

2. 模   加法  
如果最高位进位：
但结果只保留   位，则最高位进位被丢弃。
因此执行的是：
加法。
所以串行进位加法器可以实现：
 位无符号数加法；
 位补码加法。
3. 缺点  
串行进位加法器的缺点是：
也就是说：
高位加法必须等待低位进位传来。
4. 延迟分析  
每个全加器产生进位需要两级门延迟。
因此从   传到  ，一共经过   个全加器，所以进位延迟为：
级门延迟。
若一个异或门需要  3 级门延迟，则最后一位和   需要等待进位到达后再计算，因此延迟为：
级门延迟。
并行进位加法器（ CLA ）  
1. 为什么需要  CLA ？  
串行进位加法器的缺点是： 进位要从低位一位一位传到高位，速度慢 。
串行进位过程：
也就是说：进位按位逐级传递，速度慢。
C0 → C1 → C2 → C3 → C4

## 第10页

位数越多，等待进位传递的时间越长。
CLA 的思路是：
2. CLA 的核心思想  
CLA 可以根据输入  、  和初始进位  ，提前算出：
因此各位进位可以并行产生，不需要逐级等待。
这就叫：
3. 两个重要函数： G 和  P  
进位生成函数  
含义：
例如：
所以   称为进位生成函数 。
进位传递函数  
这里的   表示逻辑或。
含义：
例如：第 1 位算出  C1
第 2 位等  C1 算出  C2
第 3 位等  C2 算出  C3
第 4 位等  C3 算出  C4
不等进位逐级传递，而是提前用公式直接算出各位进位。
C1、C2、C3 、 C4
先行进位
如果 Xi = 1 且  Yi = 1 ，则本位一定产生进位。
1 + 1 = 10
只要 Xi 和  Yi 中有一个为  1 ，就能把低位传来的进位继续传到高位。

## 第11页

所以   称为进位传递函数 。
4. 每一位进位的基本公式  
第  位的进位为：
也可以写成：
含义：
5. 4 位  CLA 的进位公式  
第一位进位  
第二位进位  
含义：
第三位进位  
第四位进位  
核心理解：1 + 0 + 进位  1 = 10
0 + 1 + 进位  1 = 10
第 i 位产生进位有两种情况：
1. 本位自己产生进位： Gi = 1
2. 本位把低位进位传上来： PiCi-1 = 1
C2 可能由第  2 位自己生成；
也可能由第  1 位生成后被第  2 位传上来；
也可能由 C0 经过第  1 位、第  2 位一路传上来。
某一位的进位，要么由本位生成，要么由低位生成后一路传递上来。

## 第12页

6. 为什么  CLA 快？  
串行进位加法器中：
CLA 中：
所以  CLA 不需要一级一级等待进位传递，速度更快。
7. CLA 加法器的组成  
CLA 加法器主要由三部分组成：
第一步：计算   和  
延迟约为：
第二步： CLA 部件计算进位  
根据  、  和  计算：
延迟约为：
第三步：求和  
求和公式为：
也可以写成：C1 算完才能算  C2
C2 算完才能算  C3
C3 算完才能算  C4
C1、C2、C3 、 C4 可以通过公式并行算出
1. 进位生成  / 传递部件
2. CLA 部件
3. 求和部件
1T
C1、C2、C3 、 C4
2T

## 第13页

延迟约为：
8. 延迟计算  
和  的总延迟  
所以：
因此：
进位   的延迟  
计算   不需要经过求和部件，只需要：
所以：
因此：
9. CLA 的局限性  
CLA 速度快，但公式会随着位数增加而迅速变复杂。
例如：
因此实际中常采用：3T
生成 G/P ： 1T
CLA 算进位： 2T
求和：3T
和 S 的总延迟  = 6T
生成 G/P ： 1T
CLA 算进位： 2T
进位 C4 的延迟  = 3T
4 位、8 位  CLA 较容易实现；
16 位、32 位、 64 位  CLA 的进位逻辑会非常复杂。
局部先行进位

## 第14页

也就是：
例如：
这样可以在速度和电路复杂度之间折中。
多级先行进位加法器  
组内并行，组间串行
4 个 4 位  CLA 串联，组成一个  16 位加法器。

## 第15页

带标志加法器  
1. 带标志加法器  
普通加法器只负责计算：
但 CPU 不仅需要知道运算结果，还需要知道结果的状态，例如：
所以加法器通常会增加一些逻辑电路，用来产生标志位。
2. 常见标志位  
（1） ZF：零标志  Zero Flag  
ZF 用来判断结果是否为  0 。
例如：
ZF 常用于判断两个数是否相等，例如条件跳转指令。结果是否为  0 ？
结果是否为负数？
无符号运算是否产生进位？
有符号运算是否发生溢出？
ZF = 1：结果  F 全部为  0
ZF = 0：结果  F 不全为  0
F = 0000 0000  →  ZF = 1
F = 0000 0101  →  ZF = 0

## 第16页

（2） SF：符号标志  Sign Flag  
SF 表示结果的符号。
在补码中，最高位是符号位：
所以：
也就是结果   的最高位。
（3） CF ：进位 / 借位标志  Carry Flag  
CF 主要用于 无符号数运算 ，表示最高位是否产生进位或借位。
PPT 中给出的公式是：
对于普通加法，通常：
所以：
例如  4 位无符号加法：
只保留低  4 位结果：
最高位产生进位，所以：
说明无符号数运算超过了可表示范围。最高位为 0 ：正数或  0
最高位为 1 ：负数
  1111
+ 0001
------
1 0000
0000
CF = 1

## 第17页

标志位 主要用于 判断内容
CF 无符号数运算 是否产生进位或借位
OF 有符号补码运算 是否发生有符号溢出（4） OF ：溢出标志  Overflow Flag  
OF 主要用于 有符号补码运算 ，判断是否发生有符号溢出。
公式：
其中：
如果二者不同，则发生有符号溢出。
也可以用更直观的方法判断：
3. CF 和  OF 的区别  
核心记忆：
算术逻辑部件  ALU  
ALU 是  CPU 中负责运算的核心部件，全称为：
ALU 可以完成多种运算，例如：Cn：最高位的进位输出
Cn-1：进入最高位的进位
正数 + 正数  = 负数   →  溢出
负数 + 负数  = 正数   →  溢出
正数 + 负数         →  不会溢出
无符号数看  CF
有符号数看  OF
Arithmetic Logic Unit
算术逻辑部件

## 第18页

1. ALU 的基本结构  
ALU 通常有两个输入：
它们会同时送入不同的运算部件，例如：
这些运算结果会送入一个多路选择器  MUX 。
MUX 根据控制信号选择最终输出哪一个结果。
2. ALUop 的作用  
ALUop 是  ALU 的操作控制信号，用来决定  ALU 执行哪一种运算。
例如：
实际编码由具体  CPU 设计决定。
核心理解：
3. CarryIn 和  CarryOut  
ALU 中的加法器通常带有：加法
减法
与运算 AND
或运算 OR
异或运算 XOR
移位
扩展
截断
比较
A
B
AND 门：计算  A & B
OR 门：计算  A | B
加法器：计算  A + B
ALUop = 00 → 输出加法结果
ALUop = 01 → 输出  AND 结果
ALUop = 10 → 输出  OR 结果
ALUop 决定  ALU 当前执行什么运算。
CarryIn ：输入进位
CarryOut ：输出进位

## 第19页

它们不仅可以用于加法，也可以配合补码实现减法。
补码减法可以转化为：
其中的  +1 可以通过设置：
来实现。
4. ALU 中的标志位  
ALU 输出结果后，还会产生一些标志位：
这些标志位会被  CPU 用于条件判断和程序控制。
例如：
n位整数加 / 减如何实现 ( 补码 )  
已知：
如何计算：
因机器数用补码表示，故   为 ， 为 ，  为 ，  为 。 补 补 补 补
即已知  、，如何计算   和 ？ 补 补 补 补
补码定义  
补CarryIn = 1
ZF：结果是否为  0
SF：结果最高位，表示符号
CF：无符号运算是否产生进位 / 借位
OF：有符号运算是否溢出
判断两个数是否相等：看  ZF
判断结果是否为负：看  SF
判断无符号运算是否溢出：看  CF
判断有符号运算是否溢出：看  OF
int a = 9, b = -6;

## 第20页

补码加法  
补
补 补
补码减法  
补
补 补
如何从 求  补 补
标志位的产生与作用  
ALU中的加法器需产生标志位
零标志 ZF:F=0 时 ,ZF=1;
符号标志 SF:SF=F 的最高位 ;
(仅对有符号数有意义 , 无符号数没有意义 )
OF标志 :
4位有符号数范围是 -8~+7, 超出该范围 OF=1;
如7+6,-7-6 超出 ;-3-5 未超出 ;
(仅对有符号数有意义 , 无符号数没有意义 )
无符号数进位 / 借位标志 CF:
4位无符号数范围是 0~15, 超出该范围 CF=1;
如10+11,10-11 超出 ;10-9 则未超出。
(仅对无符号数有意义 , 有符号数没有意义 )

## 第21页

条件转移指令与标志位的关系

## 第22页

带标志位的补码加法举例  
对加减运算电路的深入认识  
1. 所有算术运算都基于 加法器实现 !
2. 加法器不关心所算的是 带符号数 还是无符号数 。
3. 加法器不判定对错 , 总是取低 n 位作为结果 , 并生成标志 信息。
原码加 /减运算  
用于浮点数 尾数运算
符号位和数值部分分开处理
仅对数值部分进行加减运算 , 符号位起判断和控制作用
规则如下 :
加法 :"同号求和 , 异号求差 ",减法 :"异号求和 , 同号求差 "
求和 :数值位相加 ,和的符号 取被加数 ( 被减数 ) 的符号。若最高位产生进位 , 则结果溢出。
求差 :被加数 ( 被减数 ) 加上加数 ( 减数 ) 的补码。
a)最高数值位 产生进位 表明加法结果为 正,所得数值位正确角。 符号位取被加数 ( 被减数 ) 的
符号 ;
b)最高数值位 没产生进位 表明加法结果为负 , 得到的是数值的 补码 ,需对结果求补 , 还原为绝
对值形式的数值位。符号位为被加数 ( 被减数 ) 的符号取反。
1. 原码运算的基本思想  
原码由两部分组成：
符号位规则：符号位 + 数值位

## 第23页

原码加减法的特点是：
即：
符号位用于判断结果正负；
数值位单独进行加减运算。
2. 原码加法规则  
3. 原码减法规则  
求差时：
4. 例  1：原码加法  
已知：
原
原
求：
原
第一步：判断真值  
原
符号位为  1，所以：
原
符号位为  1，所以：0 表示正数
1 表示负数
符号位和数值位分开处理。
同号相加：数值位相加，结果符号取相同符号。
异号相加：数值位相减，结果符号取绝对值较大的数的符号。
同号相减：数值位相减。
异号相减：数值位相加。
若最高数值位产生进位，说明结果为正，数值位正确；
若最高数值位没有产生进位，说明结果为负，得到的是补码形式，需要再求补得到绝对值。

## 第24页

第二步：判断运算规则  
两个数符号相同，都是负数，所以：
第三步：数值位相加  
数值位结果为：
结果符号取相同符号：
所以：
原
5. 例  2：原码减法  
已知：
原
原
求：
原
第一步：判断真值  同号相加，求和，和的符号取相同符号。
  0011
+ 1010
------
  1101
1101
1

## 第25页

第二步：判断运算规则  
两个数符号相同，所以：
第三步：数值位求差  
数值位为：
计算：
在 ALU 中，减法用补码加法实现：
求补
因为：
所以：
第四步：判断结果正负  
最高数值位没有产生进位，说明：
因此得到的  1001 是补码形式，需要再求补，还原成绝对值：
所以结果的数值位为：同号相减，则求差。
X 的数值位： 0011
Y 的数值位： 1010
1010 的补码  = 0110
  0011
+ 0110
------
  1001
数值位相减结果为负。
1001 求补  = 0111
0111

## 第26页

第五步：确定符号位  
由于数值位相减结果为负，所以差的符号位为   的符号位取反。原
原的符号位
取反后：
所以：
原
移码加 /减运算  
原码乘法运算  
手动计算  0

## 第27页

计算机计算  
1. 先看原码乘法的基本思想  
原码乘法用于浮点数的尾数乘法。
原码中：

## 第28页

乘法时：
也就是：
如果两个数同号，结果为正；如果两个数异号，结果为负。
这页  PPT 主要讲的是数值位怎么乘。
2. 为什么乘法可以拆成加法？  
假设乘数：
那么它的值可以展开成：
所以：
就可以写成：
这里每个   只能是  0 或  1 。
所以：
因此乘法就变成了：
所以乘法可以用：
实现。符号位 + 数值位
符号位单独处理：两个符号位异或
数值位单独相乘：看作无符号小数相乘
如果 yi = 0 ，则  X × yi = 0
如果 yi = 1 ，则  X × yi = X
判断乘数当前位是不是  1
是 1 就加  X
是 0 就加  0
再配合移位
加法器 + 移位器

## 第29页

3. 为什么  PPT 要倒序处理？  
原式是：
PPT 把它改写成从低位开始：
然后继续整理成嵌套形式：
这个形式的意义是：
所以硬件中就可以按这个规律循环。
4. PPT 中的递推公式是什么意思？  
PPT 设：
这里   表示部分积，也就是当前已经累加出来的中间结果。
然后：
意思是：
循环  n 次后，得到：每算一步，都乘一次  2^(-1) ，也就是右移一位。
从乘数最低位  yn 开始判断。
如果这一位是  1 ，就加  X ；
如果这一位是  0 ，就加  0 ；
加完后右移一位。

## 第30页

5. “判断 — 加法 — 右移 ” 具体是什么意思？  
每一轮都做三件事：
所以  PPT 最下面说：
6. 举一个简单例子  
假设：
其中：
从最低位开始处理，也就是顺序为：
第一步：初始部分积  
第二步：处理  y3 = 0  
因为：
所以：
因此：1. 判断当前乘数位  yi 是  0 还是  1
2. 如果 yi = 1 ，则部分积加  X ；如果  yi = 0 ，则部分积不加
3. 部分积右移一位
n 次“判断 — 加法 — 右移 ” ，至求出  Pn = X × Y
Y = 0.y1y2y3
y1 = 1
y2 = 1
y3 = 0
y3 → y2 → y1

## 第31页

第三步：处理  y2 = 1  
因为：
所以：
因此：
右移一位：
所以：
第四步：处理  y1 = 1  
因为：
所以：
因此：
先相加：
再右移一位：
所以：
最终：0.101 → 0.0101
  0.0101
+ 0.1010
--------
  0.1111
0.1111 → 0.01111

## 第32页

检查一下：
而：
结果正确。
看会这个例子你就 NB 了0.101₂ = 0.625
0.110₂ = 0.75
0.625 × 0.75 = 0.46875
0.01111 ₂  = 0.25 + 0.125 + 0.0625 + 0.03125 = 0.46875

## 第33页

原码两位乘法  
1. 原码乘法的基本思想  
原码乘法常用于浮点数尾数乘法。
原码乘法中，符号位和数值位分开处理：
符号位：
即：
2. 原码一位乘和两位乘  
原码一位乘：
缺点是速度较慢。
原码两位乘：符号位：两个操作数符号位异或
数值位：看作无符号数相乘
同号相乘，结果为正；
异号相乘，结果为负。
每次只判断乘数的一位。
若该位为 1 ，则加被乘数  X ；
若该位为 0 ，则不加。

## 第34页

3. 原码两位乘的规则  
每次取乘数的两位，有四种情况：
：
：
：
：
其中：
4. 为什么  11 情况要特殊处理？  
当乘数两位为  11 时，相当于要加：
但是硬件中直接求   不方便，所以可以利用：
因此：
可以改写成：
所以  11 的处理方法可以理解为：
其中：每次判断乘数的两位。
每次处理两位，所以速度大约快一倍。
P_i 表示当前部分积；
X 表示被乘数；
2^(-2) 表示右移两位。
先加 -X ，右移两位后，再加  X 。
-X 可以用补码加法实现，即加  [-X] 补。

## 第35页

5. 原码两位乘例子  
设：
原
原
求：
原
第一步：确定符号位  
X 的符号位为：
Y 的符号位为：
所以结果符号位为：
结果为负数。
第二步：计算数值部分  
数值位相乘：
这里乘数  1101 按两位分组，从低位开始看：
处理顺序为：
第三步：低两位为  01  
低两位是：
表示加：0
1
1010 × 1101
1101 = 11 01
先处理低两位  01
再处理高两位  11
01

## 第36页

所以这一部分贡献为：
第四步：高两位为  11  
高两位是：
表示加：
因为这一组在高两位，所以还要左移两位。
即：
其中：
第五步：合并结果  
低两位  01 的贡献：
高两位  11 的贡献：
所以：
130 的二进制为：
因此：1010
11
X = 1010 = 10
3X = 30
3X 左移两位  = 30 × 4 = 120
1X = 10
3X × 4 = 120
10 + 120 = 130
1000 0010
1010 × 1101 = 1000 0010

## 第37页

第六步：写出最终原码结果  
数值部分为：
符号位为：
所以：
原
补码乘法：校正法  
1. 基本思想  
校正法的思想是：
乘法过程尽量按原码乘法来做，但如果乘数为负，最后需要加一个校正项。
设补码小数：
补
补
其中， 、  是符号位。
2. 运算规则  
情况一：乘数   为正  
若：
说明乘数   为正数，可以直接按照原码乘法规则运算：
判断乘数位；
乘数位为  1 ，则加被乘数；
乘数位为  0 ，则不加；
然后移位。
即：
补 补 补10000010
1

## 第38页

情况二：乘数   为负  
若：
说明乘数   为负数。
此时处理方法是：
先去掉乘数   的符号位，用   去乘  ；
最后再加   进行校正。补
即：
补 补 补
3. 校正公式的来源  
对于补码小数，若   为负数，则：
补
又因为：
补
所以：
而：
所以：
因此：
展开得：
也就是：
所以最后要加：
补
这就是校正项。

## 第39页

4. 例题  
设：
补
补
求：
补
第一步：判断乘数   的符号  
补
符号位为  1 ，所以   是负数。
因此使用校正法：
补 补 补
第二步：先计算   
去掉乘数符号位后：
数值部分相乘：
所以：
第三步：求校正项   补
因为：
扩展为  8 位小数：
所以：
补1010 × 1101

## 第40页

第四步：加校正项  
补
计算：
1.01100000
所以：
补
5. 检查结果  
是负数补码。
其真值为：
因为：
补
所以：
结果正确。0.10000010
1.11100010

## 第41页

补码乘法： Booth 法  
1. Booth 法的基本思想  
Booth 法用于补码乘法。
它的特点是：
被乘数和乘数可以是正数，也可以是负数；
不需要像校正法一样区分乘数正负；
每次根据乘数当前最低位和附加位判断操作；
每轮之后进行一次算术右移。
核心流程：
1. 设置部分积  P = 0
2. 设置附加位  Y_-1 = 0
3. 每次观察  Y0Y_-1
4. 根据情况决定加  X、加  -X，或者不加
5. P、Y、Y_-1 整体算术右移一位
6. 重复  n 次后，P 和 Y 合起来就是乘积
2. Booth 法判断规则  
设：
P：部分积
X：被乘数

## 第42页

Y0Y_-1 操作
00 不加，直接算术右移
01 加 [X]补，再算术右移
10 加 [-X]补，再算术右移
11 不加，直接算术右移Y：乘数
Y_-1：附加位，初始为  0
每次看乘数最低位  Y0 和附加位  Y_-1。
其中：
01 表示从  0 变到  1 ，所以加  X
10 表示从  1 变到  0 ，所以加  -X
00 和 11 表示没有变化，所以不加
3. 算术右移是什么意思？  
Booth 法中要做的是补码乘法，所以右移必须是 算术右移 。
算术右移时：
如果  P 的最高位是  0，左边补  0
如果  P 的最高位是  1，左边补  1
也就是右移时要保持符号位不变。
4. 例题  
已知：
求：
先看真值：
所以理论结果应该是：[X]补 = 1101
[Y]补 = 0110
[-X]补 = 0011
[X × Y] 补
[X]补 = 1101 = -3
[Y]补 = 0110 = +6

## 第43页

8 位补码中：
下面用  Booth 法计算验证。
5. 初始化  
因为是  4 位补码乘法，所以循环  4 次。
初始：
写成整体：
6. 第  1 轮  
当前：
观察：
根据规则：
右移：
所以第  1 轮后：X × Y = -3 × 6 = -18
[-18]补 = 1110 1110
P = 0000
Y = 0110
Y_-1 = 0
P      Y      Y_-1
0000   0110   0
P      Y      Y_-1
0000   0110   0
Y0Y_-1 = 00
00：不加，直接算术右移
0000 0110 0  →  0000 0011 0
P      Y      Y_-1
0000   0011   0

## 第44页

7. 第  2 轮  
当前：
观察：
根据规则：
因为：
所以：
此时：
然后整体算术右移：
所以第  2 轮后：
8. 第  3 轮  
当前：
观察：P      Y      Y_-1
0000   0011   0
Y0Y_-1 = 10
10：加 [-X] 补
[-X]补 = 0011
  0000
+ 0011
------
  0011
P = 0011
0011 0011 0  →  0001 1001 1
P      Y      Y_-1
0001   1001   1
P      Y      Y_-1
0001   1001   1

## 第45页

根据规则：
右移：
所以第  3 轮后：
9. 第  4 轮  
当前：
观察：
根据规则：
因为：
所以：
此时：
然后整体算术右移。
注意：Y0Y_-1 = 11
11：不加，直接算术右移
0001 1001 1  →  0000 1100 1
P      Y      Y_-1
0000   1100   1
P      Y      Y_-1
0000   1100   1
Y0Y_-1 = 01
01：加 [X] 补
[X]补 = 1101
  0000
+ 1101
------
  1101
P = 1101

## 第46页

最高位是  1，所以算术右移时左边补  1：
所以第  4 轮后：
10. 最终结果  
循环  4 次结束后，取  P 和 Y 合起来作为乘积：
所以：
验证：
是负数补码，求真值：
所以：
与：
一致。
注意：这里使用的是算数右移，就是补的是最高位P = 1101
1101 1100 1  →  1110 1110 0
P      Y      Y_-1
1110   1110   0
P Y = 1110 1110
[X × Y] 补  = 1110 1110
1110 1110
取反：0001 0001
加 1：0001 0010
1110 1110 = -18
X × Y = -3 × 6 = -18

## 第47页

快速乘法器（阵列乘法器）

## 第48页

nxn位原码乘法器框图  
补码阵列乘法器  
ALU中定点除法流程  
除前预处理
1. 若被除数 =0且除数 ≠ 0,| 被除数 |<| 除数 |, 则商为 0,不再继续
2. 若被除数 ≠ 0、除数 =0,则发生 "除数为 0"异常
3. 若被除数和除数 都为 0,产生一个的 NaN。
4. 当被除数和除数 都≠0,且商≠0时,才进行除法运算。
无符号数 除法运算
被除数 (中间余数 ) 减除数来得到每一位商 够减商 1; 不够减商 0
基本操作为 减法和移位 ,故可与乘法合用同一套硬件
两个 n位数相除 , 格式统一为 : 一个 2n 位数除以一个 n 位数 ( 值不变 )
(1)定点正整数 ( 即无符号数 ) 相除 : 在被除数的高位添 m 个 0

## 第49页

(2)定点正小数 ( 即原码小数 ) 相除 : 在被除数的低位添加个
恢复余数法  
1. 基本思想  
恢复余数法是一种实现除法的方法。
它的核心思想是：
先用当前余数减去除数，判断够不够减。
如果够减，商上  1 ；如果不够减，商上  0 ，并把除数加回来恢复余数。

## 第50页

2. 为什么叫 “ 恢复余数法 ” ？  
计算机在除法过程中，通常先试着做：
余数除数
如果结果为正，说明够减，商上  1 。
如果结果为负，说明不够减，刚才不应该减除数，所以要把除数再加回来：
余数余数除数
这个 “加回来 ” 的过程就叫  恢复余数 。
3. 运算规则  
每一轮都按照下面步骤进行：
1. 当前余数减去除数；
2. 判断结果正负；
3. 若结果为正或  0 ，说明够减，商上  1；
4. 若结果为负，说明不够减，商上  0，并加回除数恢复余数；
5. 继续移位，求下一位商。
4. 核心记忆  
不恢复余数法  
余数为正：够减，商上  1
余数为负：不够减，商上  0 ，恢复余数

## 第51页

1. 基本思想  
不恢复余数法是除法运算的一种方法。
它和恢复余数法的区别在于：
如果试减后余数为负，不马上加回除数恢复余数，而是保留这个负余数，下一轮改为 “ 加除数 ” 。
所以叫  不恢复余数法 。
2. 为什么可以不恢复？  
恢复余数法中，如果余数为负，通常要：
也就是：
化简：
所以这三步可以合并成：
因此，如果余数为负，就不需要马上恢复，下一轮直接加除数即可。恢复余数 → 左移  → 再减除数
左移 → 加除数

## 第52页

3. 运算规则  
每一轮根据当前余数的正负来决定操作：
余数为正  
若：
说明够减：
公式：
余数为负  
若：
说明不够减：
公式：
4. 核心记忆  
一句话总结：
不恢复余数法就是：负余数不马上恢复，而是在下一轮通过 “ 加除数 ” 来修正。上商 1
下一步：左移后减除数
上商 0
下一步：左移后加除数
余数为正：上商  1 ，左移后减除数
余数为负：上商  0 ，左移后加除数

## 第53页

MIPS中的浮点算数运算指令  
MIPS中的浮点数传送指令  
MIPS中的浮点数比较和分支指令

## 第54页

浮点数加减法流程与特殊情况  
一、浮点数加减法的基本流程  
设两个浮点数为：
其中：
：尾数
：阶码
浮点数加减法不能直接把尾数相加，必须先让两个数的阶码相同。
二、基本步骤  
1. 求阶差  
先比较两个数的阶码大小：
结果的阶码一般先取两个阶码中较大的那个。
2. 对阶  
对阶的原则是：
小阶向大阶对齐。
也就是： 阶码小的那个数，尾数右移 。
例如：
因为   比  阶码大，所以把第二个数对齐到  ：
所以：
尾数相加：

## 第55页

3. 尾数加减  
对阶后，两个数阶码相同，就可以进行尾数加减：
加法就尾数相加，减法就尾数相减。
4. 规格化  
尾数加减后，结果可能不是规格化形式，需要调整。
规格化的目标是让尾数最高有效位为  1 ，例如：
情况一：尾数太大，需要右规  
如果尾数加法后变成：
说明最高位产生进位，需要右规：
规则：
尾数右移  1 位，阶码加  1 。
情况二：尾数太小，需要左规  
如果尾数减法后变成：
说明最高有效位不在规定位置，需要左规：
规则：
尾数左移  k 位，阶码减  k 。
三、浮点加减法中的特殊情况  
1. 对阶时可能丢失精度  
对阶时，阶码小的数要把尾数右移。
如果阶差很大，小数的尾数会右移很多位，低位可能被丢掉。
例如，假设机器尾数只能保留  4 位有效数字：

## 第56页

先对阶：
所以：
尾数相加：
但是机器只能保留  4 位有效数字，所以结果只能近似为：
这里小数的低位信息就被丢掉了。
如果阶差特别大，小数甚至完全影响不了结果，这叫：
小数被大数 “ 吃掉 ” 。
在 IEEE 754 单精度中，有效位是  24 位，所以当阶差大于约  24 时，小阶数基本就无法影响结果。
2. 对阶右移时要保留附加位  
对阶右移时，被移出去的低位不能马上全部丢掉，因为它们可能影响最后舍入。
例如，假设尾数只能保留  4 位：
阶差为：
所以第二个数尾数右移  3 位：
如果只保留  4 位，可能只剩：
但后面的   会影响最后舍入。
所以硬件会临时保留这些被移出的低位，这些位叫：
附加位。
附加位的作用是：
帮助后面进行更准确的舍入。

## 第57页

3. 右规时可能发生上溢  
如果尾数加法后最高位产生进位，需要右规：
尾数右移 ，阶码加
但如果阶码已经很大，再加  1 可能超出范围。
例如：
尾数相加：
所以：
需要右规：
但是单精度规格化数的真实阶码范围约为：
现在阶码变成了  ，超过最大范围，所以发生：
阶码上溢。
上溢说明结果太大，机器表示不了，通常会触发异常或变成无穷大。
4. 左规时可能发生下溢  
如果尾数减法后最高位为  0 ，需要左规：
尾数左移 ，阶码减
如果阶码减得太小，就会低于机器能表示的最小阶码。
例如：
尾数相减：
所以：
需要左规  3 位：
但是单精度规格化数的最小真实阶码约为：

## 第58页

现在变成了  ，超出范围，所以发生：
阶码下溢。
下溢说明结果太接近  0 ，机器可能无法用规格化数表示。
5. 尾数位数太长，需要舍入  
尾数加减后，结果尾数可能超过规定长度。
例如，假设机器尾数只能保留  4 位：
但是只能存：
后面的   放不下，就需要舍入。
如果按照就近舍入，后面的部分可能会让结果进  1 ：
所以：
尾数太长时，不能随便截断，要根据舍入规则处理。
6. 舍入后可能再次规格化  
舍入有时会让尾数产生进位。
例如尾数只能保留  4 位：
如果后面还有被舍掉的位：
舍入后可能变成：
这又不是规格化形式，所以还要右规：
所以：
舍入后也可能导致阶码加  1 。
如果阶码已经很大，这一步还可能引起上溢。

## 第59页

7. 结果尾数为  0 时，阶码也要置  0  
如果两个数相减结果为  0 ，例如：
尾数相减：
结果就是  0 。
这时不能保留原来的阶码  ，而应该把整个浮点数置为  0 ：
阶码 尾数
因为：
尾数为  0 ，说明整个结果就是  0 ，阶码已经没有意义。
8. 偏置常数不影响阶差计算  
IEEE 754 单精度浮点数的阶码采用移码表示，偏置常数为  127 。
如果真实阶码为：
那么存储的阶码为：
求阶差时：
中间的  127 会抵消：
所以：
偏置常数不会影响阶差计算。
例如：
真实阶差：
存储阶码：
用存储阶码相减：

## 第60页

结果一样。
四、浮点加减法核心总结  
浮点加减法流程：
求阶差对阶尾数加减规格化舍入

## 第61页

浮点加减运算中的附加位、舍入方式和溢出判断  
一、附加位  Extra Bits 是什么？  
浮点数的尾数位数是有限的。比如  IEEE 754 单精度尾数实际有效位是  24 位，但是在运算过程中，尾数
右移、加减、规格化之后，可能会多出一些低位。
这些多出来的低位如果直接扔掉，结果可能不准确，所以需要临时保留一些位，叫  附加位。
PPT 里提到至少有两个附加位：
可以理解成：
例如机器最后只能保留：
但是运算结果是：
那么：
这两个附加位的作用是： 帮助判断最后到底是舍掉，还是进一位。Guard bit ：保护位，紧挨着有效尾数的右边
Rounding bit ：舍入位，在保护位的右边
有效尾数 | G | R
1.1101
1.1101 01
1.1101 是最终要保留的尾数
0 是 Guard bit
1 是 Rounding bit

## 第62页

二、附加位为什么重要？  
比如对阶时，小阶数的尾数要右移：
如果要对齐到：
尾数要右移  3 位：
如果机器只能保留：
后面的  111 直接丢掉，会影响精度。
所以要把右移出去的低位暂时留下，作为附加位。后面舍入时，再根据这些附加位决定结果是否进位。
一句话：
附加位就是为了防止对阶右移时把重要低位直接丢掉。
三、附加位在规格化中的作用  
PPT 里说：
左规时移到尾数中；或作为舍入的依据。
什么意思？
如果尾数加减后需要左规，比如：
要规格化成：
这时原来在右边的一些附加位，可能会因为左移而进入有效尾数范围。
所以附加位不只是 “ 等着被舍入 ” ，有时候还会因为左规进入真正的尾数中。1.111 × 2^2
× 2^5
1.111 × 2^2 = 0.001111 × 2^5
0.001
0.001101
1.101 × 2^(-3)

## 第63页

四、 IEEE 754 的舍入方式  
舍入就是：当结果尾数太长，机器存不下时，按照规则取一个可表示的近似值。
假设真实结果是  Z，但机器只能表示离它最近的两个数：
其中：
IEEE 754 常见舍入方式有四种。
1. 向  + ∞  方向舍入  
也叫向正无穷舍入。
意思是：结果往更大的方向取。
所以：
例如：
对于负数，向  + ∞  是往 “ 没那么负 ” 的方向。
例如：
2. 向  -∞  方向舍入  
也叫向负无穷舍入。
意思是：结果往更小的方向取。
所以：
例如：
负数：Z1   Z   Z2
Z1 是 Z 左边较小的可表示数
Z2 是 Z 右边较大的可表示数
舍入为 Z2
1.234 → 1.24
-1.234 → -1.23
舍入为 Z1
1.234 → 1.23

## 第64页

3. 向  0 方向舍入  
也叫截断。
意思是：直接把多余位截掉，往  0 靠近。
所以：
例如：
它的特点是： 不管正负，绝对值都变小。
4. 就近舍入  
这是默认方式。
意思是：选择离真实结果最近的那个可表示数。
如果离  Z1 更近，就取  Z1 ；如果离  Z2 更近，就取  Z2 。
如果刚好在中间，就采用 “ 取偶 ” 原则。
五、 PPT 里的  01 、 11 、 10 是什么意思？  
PPT 这里讲的是根据附加位判断舍入。
假设只保留  4 位尾数，多出来的两位是附加位。
情况  1：附加位是  01  
保留部分：
附加位：
01 比半个单位小，所以直接舍掉。-1.234 → -1.24
正数：取 Z1
负数：取 Z2
+1.234 → +1.23
-1.234 → -1.23
1.110101 → 1.1101
1.1101
01

## 第65页

情况  2：附加位是  11  
保留部分本来是：
附加位是：
说明被舍掉的部分比较大，要进位。
所以：
情况  3：附加位是  10  
10 表示刚好在两个可表示数中间。
这时用 “强迫结果为偶 ” 的规则，也就是看保留尾数的最低位。
如果最低位是  0  
保留部分：
最低位是  0，已经是偶数，所以舍掉多余位。
如果最低位是  1  
保留部分：
最低位是  1，不是偶数，所以进  1 ：
所以  10 的规则是：1.110111 → 1.1110
1.1101
11
1.1101 + 0.0001 = 1.1110
1.111010 → 1.1110
1.1110
1.110110 → 1.1110
1.1101
1.1101 + 0.0001 = 1.1110

## 第66页

目的就是让最终结果最低位为偶数，也就是最低位为  0 。
六、溢出判断  
浮点数规格化时，阶码可能会改变：
所以可能产生阶码溢出。
1. 右规可能导致上溢  
右规发生在尾数太大的时候。
例如：
结果尾数变成：
不是规格化形式，需要右规：
也就是：
如果阶码本来已经很大，再加  1 就可能超出范围，这叫  上溢。
PPT 里的判断规则是：
因为在  IEEE 754 里，阶码全  1 通常是保留给无穷大和  NaN 的，不再表示普通规格化数。如果最低位为  0 ：舍
如果最低位为  1 ：入
右规：尾数右移，阶码加  1
左规：尾数左移，阶码减  1
1.111 + 1.111 = 11.110
11.110
11.110 × 2^E = 1.1110 × 2^(E+1)
尾数右移 1 位，阶码加  1
右规前先看阶码是否全  1 ；
如果已经全  1 ，直接认为上溢。
否则阶码加  1 ；
加 1 后如果变成全  1 ，也认为上溢。

## 第67页

2. 左规可能导致下溢  
左规发生在尾数太小的时候。
例如：
结果是：
不符合规格化形式，要左规：
也就是：
如果阶码本来就很小，再减就可能低于最小表示范围，这叫  下溢。
PPT 里的判断规则是：
因为在  IEEE 754 里，阶码全  0 通常表示  0 或非规格化数，不再是普通规格化数。
 1.001 - 1.000 = 0.001
0.001 × 2^E
0.001 × 2^E = 1.000 × 2^(E-3)
尾数左移 3 位，阶码减  3
左规前先看阶码是否全  0 ；
如果已经全  0 ，直接认为下溢。
否则阶码减  1 ；
减 1 后如果变成全  0 ，也认为下溢。

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_计算机组成原理_课程MOC|计算机组成原理 MOC]]

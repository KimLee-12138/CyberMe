---
id: extract-co-b924025d
type: extract
status: extracted
course: CO
source:
  - "[[91_Raw-Archive/PDF/CO_计组还原卷_2025去年卷_修正版.pdf_课件_未知日期_b924025d.pdf]]"
source_pages: all
source_hash: "b924025d12f0173269878da777a6ea3aedb9e006b7b3fd70603a500f02249f4d"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 计组还原卷_2025去年卷_修正版.pdf：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

## 第1页

2025 年《计算机组成原理》去年卷还原版
（修正版） 
你说  25 和  25(1) 是去年卷，最有参考价值。
由于手写照片部分文字模糊，本文件按 “ 可读内容  + 老师录音重点  + PPT 依据 ” 修正。
看不清的题干继续保留【按考点还原】标记。
一、基础题  / 填空题  / 选择题  
1. 定点数与浮点数表示  
题目还原：
定点数和浮点数的表示特点是什么？浮点数为什么表示范围更大？
答案：
定点数的小数点位置固定，表示范围较小，但数值间隔均匀；浮点数由符号、阶码和尾数组成，小数点
位置可随阶码浮动，因此表示范围大，但数值分布不均匀。
浮点数通式：
IEEE 754 单精度：
2. 奇偶校验和  CRC  
题目还原：
给定  6 位数据  100011 ，求奇偶校验位；并使用  CRC ，生成多项式：
求 3 位校验码。
（1 ）奇偶校验  
数据：
1 的个数：N = (-1)^S × M × 2^E
1 位符号位  + 8 位阶码  + 23 位尾数
G(x) = x^3 + 1
100011
3 个

## 第2页

答案：
解析：
偶校验要求总的  1 的个数为偶数，原来有  3 个  1 ，所以补  1 。
奇校验要求总的  1 的个数为奇数，原来已经是奇数，所以补  0 。
（2 ） CRC  
生成多项式：
对应二进制除数：
原数据后补  3 个  0 ：
模 2 除法余数：
所以  CRC 校验码：
发送码字：
3. MIPS 、 x86 、 ARM 与  RISC/CISC  
题目还原：
MIPS 指令集特点是什么？ x86 和  ARM 分别属于什么类型？
答案：
RISC 特点：偶校验位 = 1
奇校验位 = 0
G(x) = x^3 + 1
1001
100011000
111
111
100011111
MIPS：RISC
ARM：RISC
x86：CISC

## 第3页

控制器 优点 缺点
硬布线控制器 速度快 设计复杂，修改困难
微程序控制器 规整，易修改，适合复杂指令 速度较慢CISC 特点：
4. 微程序控制器  
题目还原：
微程序控制器如何产生控制信号？
答案：
微程序控制器把机器指令的执行过程分解为一串微操作，并用微程序实现。微程序存放在控制存储器  
CS 中；执行时由  μ PC 给出微指令地址，取出微指令送入  μ IR ，微指令经译码产生控制信号，即微命
令。
流程：
对比：
二、程序与数据表示题  
题 5 ： union 判断大小端  
题目还原：指令少、格式规整、通常定长、寄存器较多、 load/store 结构、便于流水线。
指令多、格式复杂、寻址方式多、常用微程序控制、兼容性强。
机器指令
↓
起始地址发生器产生微程序入口
↓
μPC 指向 CS 中的微指令
↓
取微指令到  μIR
↓
μIR 产生控制信号
↓
控制数据通路完成微操作

## 第4页

（1 ）小端存储  
假设：
小端：低地址存低字节。
所以：
输出：
（2 ）大端存储  
大端：低地址存高字节。
所以：
输出：union test {
    int a;
    char b;
};
int main() {
    union test test;
    test.a = 0xff;
    if (test.b == 0xff)
        cout << "小端";
    else
        cout << "大端";
}
int 32 位
test.a = 0x000000FF
低地址 → 高地址
FF 00 00 00
test.b = 0xFF
小端
低地址 → 高地址
00 00 00 FF
test.b = 0x00
大端

## 第5页

（3 ）大端机器上想让  test.b == 0xff  
答案：
解析：
大端模式下， char b 取联合体起始地址的第一个字节，也就是  int 的最高有效字节，因此应把最高字
节设为  FF 。
三、指令系统题  
6. 指令条数与操作码位数  
题目还原：
如果某指令系统共有  80 条指令，操作码至少需要多少位？
答案：
因为：
7. MIPS 指令格式与跳转 / 分支  
MIPS 三种格式：
R 型：
I 型：
J 型：
常见指令：test.a = 0xFF000000
ceil(log2 80) = 7 位
2^6 = 64 < 80
2^7 = 128 ≥ 80
op | rs | rt | rd | shamt | funct
6    5    5    5     5       6
op | rs | rt | imm16
6    5    5    16
op | target26
6    26

## 第6页

分支目标地址：
J 型跳转目标地址：
四、磁盘题  
8. 磁盘访问时间计算  
题目还原：
某磁盘每扇区  512B ，转速  5400 RPM ，平均寻道时间  12ms ，数据传输速率  4MB/s ，求平均访问时
间。
（1 ）一圈时间  
（2 ）平均旋转等待时间  
（3 ）传输一个扇区时间  
若按：
则：add rd, rs, rt     R[rd] ← R[rs] + R[rt]
sub rd, rs, rt     R[rd] ← R[rs] - R[rt]
ori rt, rs, imm    R[rt] ← R[rs] OR ZeroExt(imm)
lw rt, imm(rs)     R[rt] ← M[R[rs] + SignExt(imm)]
sw rt, imm(rs)     M[R[rs] + SignExt(imm)] ← R[rt]
beq rs, rt, imm    if R[rs]=R[rt] then branch
j target           jump
BranchTarget = PC + 4 + (SignExt(imm16) << 2)
JumpTarget = { PC+4 的高  4 位 , target26, 00 }
5400 RPM = 5400 / 60 = 90 转 / 秒
一圈时间 = 1 / 90 s ≈ 11.11 ms
平均旋转等待时间  = 一圈时间  / 2
≈ 5.56 ms
1MB = 2^20B
传输时间 = 512 / (4 × 2^20) s
≈ 0.122 ms

## 第7页

（4 ）平均访问时间  
五、 Cache 题  
题 9 ：直接映射  Cache 地址划分  
题目还原：
要求：
1. 地址划分；
2. 访问地址对应的  Tag 、行号、块内地址；
3. 包括  Valid 和  Tag 后的  Cache 总容量；
4. 改成  2 路、 4 路、全相联时  Tag 如何变化。
（1 ）地址划分  
主存容量：
主存地址：
块大小：
块内地址：
Cache 行数：平均访问时间  = 平均寻道时间  + 平均旋转等待时间  + 数据传输时间
≈ 12 + 5.56 + 0.122
≈ 17.68 ms
Cache 容量  = 8KB
主存容量 = 1024KB
块大小 = 512B
每行有 1 位  Valid bit
直接映射
访问地址： 0C63BH
1024KB = 1MB = 2^20B
20 位
512B = 2^9B
9 位
8KB / 512B = 8192B / 512B = 16 行  = 2^4

## 第8页

行号：
Tag ：
地址格式：
（2 ）地址  0C63BH 的划分  
按 Tag 7 | 行号  4 | 块内地址  9 划分：
因此访问  Cache 第  3 行，检查：
（3 ） Cache 总容量  
数据容量：
每行附加位：
16 行附加位：
总容量：
换算字节：4 位
20 - 4 - 9 = 7 位
Tag 7 位  | 行号  4 位  | 块内地址  9 位
0C63BH = 0000 1100 0110 0011 1011B
Tag      = 0000110B = 06H
行号     = 0011B = 3
块内地址 = 000111011B = 03BH
Valid 是否为  1
Tag 是否等于  06H
8KB = 8192B = 65536 bit
Tag 7 位  + Valid 1 位  = 8 位
16 × 8 = 128 bit
65536 + 128 = 65664 bit

## 第9页

映射方式 组数 组号位数 Tag 位数
直接映射 16 4 20 - 4 - 9 = 7
2 路组相联 8 3 20 - 3 - 9 = 8
4 路组相联 4 2 20 - 2 - 9 = 9
全相联 1 0 20 - 0 - 9 = 11
指 令 MemtoReg RegWr MemWr RegDst ExtOp ALUSrc Branch Jump ALUOp
R-type
add/sub0 1 0 1 x 0 0 0001 /
funct
ori 0 1 0 0 0 1 0 0 010
lw 1 1 0 0 1 1 0 0 000
sw x 0 1 x 1 1 0 0 000
beq x 0 0 x x 0 1 0 100
jump x 0 0 x x x 0 1 xxx（4 ）不同映射方式  Tag 长度  
数据容量仍为  8KB ，块大小仍为  512B ，总块数仍为  16 。
结论：
六、 CPU 数据通路与控制信号题  
10. add/sub/lw/sw/beq/jump/ori 的控制信号  
11. 用  ALU 实现  add 和  sub  
加法：
减法：
硬件：65664 / 8 = 8208B
相联度越高，组号位越少， Tag 位越多；
数据容量不变，但  Tag 等附加位增加，所以实际硬件总容量略增。
A + B
A - B = A + (~B) + 1

## 第10页

12. 有符号溢出判断  
加法溢出：
减法转加法判断：
注意：
七、程序执行流程题  
13. hello world 从源代码到执行结果  
答案：
从组成原理角度：
2025 去年卷最重要复习点  Sub=0：B 原样进入加法器， Cin=0
Sub=1：B 取反进入加法器， Cin=1
正 + 正 = 负
负 + 负 = 正
A - B = A + (-B)
add/sub/addi 可能产生溢出异常；
addu/subu 不按有符号溢出处理。
1. 编辑源程序
2. 预处理
3. 编译：高级语言  → 汇编语言
4. 汇编：汇编语言  → 目标机器代码
5. 链接：目标文件  + 库函数  → 可执行文件
6. 装载：操作系统把可执行文件装入内存
7. CPU 执行： PC 取指、 IR 保存指令、控制器译码、数据通路执行
8. 通过系统调用和  I/O 输出  hello world
高级语言程序最终变成机器指令；
机器指令和数据装入主存；
CPU 根据 PC 取指，控制器产生控制信号；
ALU、寄存器和存储器配合执行；
最后通过 I/O 系统输出结果。

## 第11页

1. IEEE 754、定点 / 浮点区别
2. 奇偶校验、 CRC
3. RISC/CISC， MIPS/ARM/x86
4. 微程序控制器
5. 大小端与  union
6. 操作码位数计算
7. 磁盘访问时间
8. Cache 地址划分、 Tag 、 Valid 、直接映射 / 组相联 / 全相联
9. MIPS 指令格式、分支跳转地址
10. 单周期  CPU 控制信号

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_计算机组成原理_课程MOC|计算机组成原理 MOC]]

---
id: extract-py-3c00ecee
type: extract
status: extracted
course: PY
source:
  - "[[91_Raw-Archive/DOC/PY_python复习陈.docx_资料_未知日期_3c00ecee.docx]]"
source_pages: all
source_hash: "3c00eceedfe3e327f25d98302ee0e65578250c14d7c33b5fe413cd601d432e62"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# python复习陈.docx：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

python复习

组合数据类型：

序列类型常用操作符和函数

1.序列类型一般有：字符串、元组、列表，序列的下标默认从0开始

2.序列操作符：

in     not in +   s*n   len   min  max  s[i]   s[i:j]  s[i:j:k]     sum(s)      s.index(x)     s.rindex(x)    s.count(x) 

3.序列为可迭代对象

字符串：

1.单引号和双引号作为界定符（是不是字符串）

2.单引号内可以用双引号，此时双引号被视为普通字符，反之也成立

3.字符串的打印是不会输出引号的，因为只是界定符

字符串的切片操作：

1.s[i:j]是截取的从下标为0的位置到下标为j-1的位置，不会到j

2.[:]是默认截取全部字符

3.[6:]从下标为6的位置截取到结尾

4.str[:-3]从开头截取到下标为-3这个字符之前

5.[::-1]创建一个与原字符串顺序相反的字符

6.[i:j:k]k为正代表从左向右截取，k为负代表从右向左截取

字符串的操作

Python中的split：

字符串控制精度

m.n      m是最小的总宽度，n是小数点后的位数

如果说没有出现小数点，那么只有整数部分

%4d控制整数部分只有4位

%.2f控制小数部分只有2位，整数部分不控制

字符串转义字符

\n换行

\\的转义字符，代表的是路径中的\

正常情况下内外引号不一致可以

若想用同样的引号则需要进行转义

序列

1.序列是可改变类型

2.序列增加用append但只能追加一个元素，追加多个元素用+=

3.修改可以用list[下标]=，不能对空列表进行修改

4.删除用del或者remove（删除第一个匹配项）

5.列表的常见操作

元组：

1.元组与列表类似，但元组是不可变类型

2.元组用（）

3.即使是只含一个元素也要在元素后面添加，否则如果不加逗号识别不了为元组，加了逗号就正确

4元组不允许修改，但是可以对元组进行拼接组合

5.元组可用del语句删除整个元组，但是删除整个元组后不能再输出

集合类型

集合元素是可变类型

字典类型--映射类型

1.字典是可变容器型模型，键/值对，每对用逗号分隔开，整个字典放在花括号中

2.字典的创建

可以字符做键创建，也可用数字做键创建

3.字典的访问用dict1[键]（键）

(1)如果是访问数字键那么不用加' '，如果是访问字符键，那么要加' '/" "

(2)如果所用键不在字典中，数据无法访问，输出错误信息

4.字典的添加

直接增加键值对

dict.update()

5.字典的删除

可以用del命令，删除一整个字典或者删除字典中某个键值对

也可用clear删除整个字典元素，输出为空字典{}

当整个字典被删除后，不能访问其中的键值对

6.字典中的函数

   str（以可打印的字符串表示）

   len（计算字典中元素个数）

   type（）如果是字典返回字典类型

   clear清楚字典

字典中的浅拷贝

copy返回浅拷贝

也就是浅拷贝是可能会受影响的，

取决于x中的键的值是不是可变对象（列表类的），如果是，那么修改它的值，原对象也会受影响，因为两个对象共用一个对可变对象的引用

但如果值本来为不可变对象，那么在y中修改了键的值，x是不会受影响的

深拷贝

其他函数

fromkey（[序列]，（逗号后面是键的值））以序列中的元素为字典的键，以后面的值为每个键赋值

get返回指定键的值，若值不在字典中，返回default的值

Key in dict若键在字典中，返回true，不在返回false

items以列表返回可遍历的元组数组

keys以列表返回一个字典的所有键

setdefault与get类似，但若键不在字典中，会将键添加并且把值设置为default

update把字典2的键更新在字典中

values以列表返回字典中所有值

字典中的注意事项:

1.字典中值可以任意类型，键必须是不可变类型，数组，字符串，元组做键，列表不行

2.字典中的键不能出现两次，如果一个键被两次赋值，后一次赋值会被记住

3.dict.items() 返回一个包含字典中所有键值对的视图对象。这个视图对象可以用于迭代字典的键值对，每个键值对以元组的形式表示 (key, value)

实验大题操作:

控制结构

if-elif-else语句：

适用于多个条件的选择

三元运算符x if c else y：

比较操作符

== 运算符用于比较变量的值是否相等，is 运算符用于比较变量的身份标识是否相同。

a和c共用一个列表的地址，因此is值是相同的，而a和b虽然值相同，但是是两个不同的列表，因此不相同

if嵌套语法

判断输入字符的类型

需要使用 str() 函数将用户输入的值转换为字符串类型，以便正确地应用 ord() 函数获取对应的 Unicode值

for循环结构

0-1=-1，以此类推

当迭代的序列为空的时候执行else语句，循环正常完成以后也会执行

当循环里面有了break执行了以后就不会再执行else了

while循环实例

数据库

文件处理

rt文本方式打开，rb二进制

read的使用

write的使用

文件基本操作

读图片

用二进制模式读写图片

文件指针

文件的截断操作

注意必须在r+，读写模式下才能进行截断，会把文本截断到规定的大小

文件的基本操作

1复制文件

记得两个文件地址都要在前面加r

2.文件的删除

3.获取当前工作目录

4.获取当前目录的内容

别忘了__name__ 和__main__是双下划线

定义函数gef(dir_path)：该函数接收一个参数dir_path，表示目录路径。

在函数内部，使用os.path.exists(dir_path)判断指定目录是否存在。如果目录存在，执行以下步骤；否则返回字符串提示目录不存在。

使用os.listdir(dir_path)获取指定目录下的文件和文件夹列表，并返回该列表。

需要使用if __name__ == '__main__':语句块的情况是希望一部分代码只在当前模块作为主程序执行时执行，而不希望在模块被导入时执行。而对于模块中的其他代码，可以直接放在模块的顶层

CSV文件的处理

1.写csv文件

2.读csv文件

excel文件的处理

读文件

写文件

1.写excel文件

读取excel文件的最后一列：

方法一：先读取excel文件中的所有工作表，然后再循环读取每个工作表的每行，然后再输出每行的最后一列s.row(i)[-1].value;

方法二：

通过wb.nsheets获取工作表的数量，使用range(wb.nsheets)循环遍历每个工作表的索引。

在循环中，通过wb.sheet_by_index(i)获取当前索引对应的工作表对象，并将其赋值给变量s。

然后，打印出当前工作表的名称，使用s.name获取工作表的名称。

接下来，在内部循环中，使用s.ncols-1获取最后一列的索引，然后使用s.col_values方法获取该列的所有值。s.col_values返回一个列表，其中包含了该列中所有单元格的值。

（s.ncols-1表示最后一列的索引。在Excel文件中，列索引从0开始计数，所以最后一列的索引是总列数减1。通过s.ncols-1，我们可以获取最后一列的索引，然后可以使用该索引来访问或处理最后一列的数据。-1和s.ncols-1不完全一样。在xlrd库中，可以使用-1作为索引来表示最后一列。因此，将s.ncols-1改为-1可以得到相同的结果，访问最后一列的数据。这是xlrd库的一种便利用法，但在其他情况下，通常使用-1表示最后一个元素的索引，而不仅仅局限于列索引。）

文件的修改

delete删除的是表的数据，drop删除的是整个表的结构

c从teacher表中获得所有数

数据库编程

sqlite3：

1.基本操作

创建数据库的表

在cmd里面的操作

mysql

cmd

python：

GUI编程

Tkinter模块

Label组件

1.可以用于显示图标和文本

最后一定别忘了mainloop

text组件

text组件用于写多行文字

file=‘ 这里面是绝对路径’

Fg是文本颜色，bg是背景颜色，button中的text是按钮的文字，点击会进入command对应的函数

b1.pack()等价于b1=（）.pack

验证一下

从 最后插入

可以做一个学生管理信息管理系统

这个就是下拉菜单的菜单项

这个是让下拉菜单栏和最上面的一栏产生联系。比方说信息管理的下拉菜单看就是sm1

stick是靠近的方向

横跨两列

ns是拉伸高度，ew是拉伸宽度，nesw整个拉伸

垂直滚动条

第0行第2列跨4行

显示几个数据

网络爬虫

Request

1.response.get是requests库中的一个方法，用于从服务器响应中获取特定的信息或属性。response.get并非一个单独的方法，而是指通过响应对象获取相应属性（网页内容、状态码、响应头等信息）的一种方式，例如response.text用于获取文本内容。

reques 的请求方式

GET请求：使用requests.get(url)方法发送GET请求。GET请求用于从服务器获取数据，将请求参数附加在URL的查询字符串中。

POST请求：使用requests.post(url, data)方法发送POST请求。POST请求用于向服务器提交数据，将请求参数作为data参数传递给requests.post()方法。

requests.put

requests.delete

requests.head

requests.options

requests获取网页数据

爬取豆瓣电影排行榜

爬取壁纸

数据分析

Numpy

1.创建数组，使用array函数，接收的是一切序列对象，生成一个含有传入数据的数组

np.array

2.创建多维数组

3.生成数组时，可指定生成的数据类型

4.创建特殊数组

求最大值，最小值，平均值

pandas

1.创建series对象，serise类似的是一维数组，由一、索引和值构成，索引在左边值在右边

默认的索引值为0下标开始0...n-1,可以自己设定索引值，index=["  "," "]

可以通过列表，元组，字典来建立series

2.pandas的自动对齐

3.pandas中的数据结构DataFrame

（1）DataFrame会将字典的键作为列标签，字典的值作为对应列的数据。因此，name、age和status在这里被解释为列名。

(2）每个series对象的索引都被用作列标签

4.pandas 读取csv文件

5.pandas删除数据

当然！下面是每行代码的中文解释：

df['date'] = pd.to_datetime(df['date']): 这行代码使用pd.to_datetime()函数将DataFrame df中的 'date' 列转换为日期时间格式。这样做是为了确保日期以正确的日期时间对象进行进一步的分析。

daily_sales = df.groupby('date')['amount'].sum(): 这行代码按照 'date' 列对DataFrame df进行分组，并计算每个日期对应的 'amount' 列的总和。结果对象 daily_sales 是一个包含每日销售额的Series。

plt.plot(daily_sales.index, daily_sales.values): 这行代码使用plt.plot()函数创建一个折线图。它以daily_sales Series的索引（包含日期）作为x轴值，以daily_sales Series的值（包含销售额）作为y轴值。

plt.xlabel('日期'): 这行代码将x轴的标签设置为'日期'。

plt.ylabel('销售额'): 这行代码将y轴的标签设置为'销售额'。

plt.title('每日销售额'): 这行代码将图表的标题设置为'每日销售额'。

plt.xticks(rotation=45): 这行代码将x轴刻度标签旋转45度，以防止当日期较多时重叠。

这些代码共同创建了一个折线图，展示了随时间变化的每日销售额。

matplotlib库

import matplotlib.pyplot as plt：这行代码导入matplotlib库的pyplot模块，并将其命名为plt。它允许我们创建和自定义图形。

import numpy as np：这行代码导入numpy库，并将其命名为np。它提供了处理数组和数学函数的支持。

x = np.linspace(0, 10, 1000)：这行代码使用numpy的linspace函数生成一个包含1000个在0到10之间等间隔的数值的数组x。

y = np.sin(x) + 1：这行代码对数组x中的每个元素计算sin(x)的值，并将结果加1。它表示第一条曲线的y值。

z = np.cos(x**2) + 1：这行代码对数组x中的每个元素计算cos(x^2)的值，并将结果加1。它表示第二条曲线的y值。

plt.figure(figsize=(8, 4))：这行代码创建一个新的图形，设置大小为8英寸（宽度）和4英寸（高度）。它设置了图形的尺寸。

plt.plot(x, y, label='sinx+1', color='red', linewidth=2)：这行代码绘制由x和y定义的曲线。它将曲线的标签设置为'sinx+1'，颜色设置为红色，线宽设置为2。

plt.plot(x, z, label='cos^2+1')：这行代码绘制由x和z定义的曲线。它将曲线的标签设置为'cos^2+1'。默认的颜色和线宽被使用。

plt.xlabel('Time(s)')：这行代码设置x轴的标签为'Time(s)'。

plt.ylabel('Volt')：这行代码设置y轴的标签为'Volt'。

plt.title('A simple example')：这行代码设置图形的标题为'A simple example'。

plt.ylim(0, 2.2)：这行代码设置y轴的显示范围为0到2.2。

plt.legend()：这行代码在图形上显示图例，显示绘制曲线的标签。

plt.show()：这行代码在屏幕上显示图形。

总体而言，这段代码生成一个包含两条曲线的图形：sin(x)+1和cos^2(x)+1。图形具有带有标签的坐标轴、标题、图例以及特定的大小和范围设置，以便更好地可视化。

使用案例

绘制折线图

绘制柱状图，按月份进行统计，显示商店每月的销售情况

创建折线图

创建柱状图

Scipy

1.求解线性方程组

求解线性方程组：

进行图像处理

第1行：导入ndimage模块用于图像处理，导入misc模块用于获取示例图像。

第2行：导入pylab模块并重命名为pl，用于绘图和显示图像。

第5行：使用misc.ascent()函数生成一个名为ascent的白噪声图像。

第8行：使用ndimage.shift()函数对图像进行平移操作，将图像平移50个像素，并保存在shifted_ascent变量中。

第11行：使用ndimage.shift()函数对图像进行平移操作，将图像平移50个像素，并使用"nearest"模式进行填充，保存在shifted_ascent2变量中。

第14行：使用ndimage.rotate()函数对图像进行逆时针旋转30度，保存在rotated_ascent变量中。

第17行：使用pl.imshow()函数展示原始灰度图像ascent。

第20行：创建一个大小为4x3英寸的新图像窗口，并展示平移处理后的图像shifted_ascent（未自动填充）。

第23行：创建一个大小为3x4英寸的新图像窗口，并展示平移处理后的图像shifted_ascent2（自动填充）。

第26行：创建一个大小为2x5英寸的新图像窗口，并展示旋转处理后的图像rotated_ascent。

第29行：使用pl.show()函数显示图像。

3.使用箱型图检测异常值

第1-3行：导入所需的库和模块。

第6行：创建一个包含数据的DataFrame对象，将数据存储在名为value的列表中。

第9行：创建一个新的图像窗口。

第12行：使用data.boxplot()函数绘制箱线图，并将绘图结果以字典形式保存在变量p中。

第15行：从箱线图结果中获取离群点的x坐标和y坐标。

第16行：对离群点的y坐标进行排序，以便进行标注。

第19-24行：遍历离群点数据，使用plt.annotate()函数在图像上进行标注，显示离群点的值。

第27行：使用plt.show()函数显示图像。

4.缺失值分析

python写代码时tips

1.想要输出的内容在一行

这样i和n就是输出在一行，并以逗号进行间隔开

2.输出列表中元素的编号，编号即下标，从0开始

3.任意输入一个字符串，代表的可能是某个元素，eg:任意输入一种淡水鱼名称，如'黑鱼'

4.在循环中找到了所需要的元素或者值时，可以及时用break退出

5.输入一个数字

基础语法题

考点：random模块的random函数

random.random()函数可以生成一个[0，1)之间的随机小数，乘以30+10是【10,40)，因此不包含40，所以应该是*31

考点：函数的传参规则

在函数调用中，如果使用了关键字参数，那么该参数必须在位置参数之后给出。在函数定义中，参数x是位置参数，而y是具有默认值的关键字参数。因此，调用函数test时，只能先给出位置参数x，然后再给出关键字参数y。

考点：类属性

单下划线是保护属性，双下划线是私有属性，后缀下划线是为了放置和内置的属性重名


该程序的输出结果是 C. [1 2 3 4 5]。代码中使用了NumPy库的array()函数将列表data转换为NumPy数组arr，然后使用print()函数打印数组。NumPy数组在打印时会以类似 [1 2 3 4 5] 的格式显示。

考点：浮点数宽度精度的控制，

m.n，m是控制的总的最小宽度，可以大于它（大于它是没有影响的），如果小于它，那么会在左侧填充空格

考点：lambda表达式，lambda函数是一种简洁的函数定义方式，用于创建单行的简单的函数

表达式 f = lambda x, y: x // y 定义了一个匿名函数，该函数接受两个参数 x 和 y，并返回 x 除以 y 的整数部分（整数除法结果）。

在这个函数定义后，调用 f(7, 2) 的结果是 3。这是因为 7 除以 2 的整数部分为 3。

list2=[item for item in list1 if isprime(item)]

使用列表推导式创建了一个新的列表list2。在列表推导式中，对于list1中的每个元素item，调用isprime(item)函数进行判断。如果isprime(item)返回True，则将item添加到list2中。

根据给定的代码，se是一个Pandas Series对象，其中包含了一个字典obj作为数据。通过索引操作se[2][2][3]，我们可以得到字典中的值。

根据字典obj的结构，se[2]表示获取字典中索引为2的元素，即['C', 3, {3: 'c'}]。接着，[2]表示获取字典中索引为2的元素，即{3: 'c'}。最后，[3]表示获取字典中键为3对应的值，即'c'。

因此，程序输出的结果是 'c'。


根据给定的代码，df是一个Pandas DataFrame对象，其中包含了一个字典obj作为数据。通过索引操作df["A"][2][1]，我们可以获取DataFrame中指定元素的值。

首先，df["A"]表示获取DataFrame中"A"列的数据，这是一个列表。然后，[2]表示获取列表中索引为2的元素，即['C', 3]。最后，[1]表示获取列表中索引为1的元素，即3。

因此，程序输出的结果是 3。

根据给定的代码，函数test接受三个参数：列表a，索引index和值value。该函数的目标是将列表a中索引为index的元素设置为value。如果索引超出了列表的范围（即引发了IndexError异常），则将message设置为"study hard"，否则设置为"success"。最后，函数返回message。

在主程序中，创建了列表a为[1, 2, 3]，设置了index为4和value为10。然后调用test函数，将返回的结果存储在msg中，并打印msg。

由于索引4超出了列表a的范围，所以会引发IndexError异常。在异常处理中，将message设置为"study hard"。因此，程序输出的结果是 "study hard"。


输出结果为：['GOOD', 'MORNING']

split() 是字符串的方法，用于将字符串按照指定的分隔符分割成多个子串，并返回一个包含分割后子串的列表。如果不指定分隔符，默认使用空格作为分隔符进行分割。

在上述代码中，字符串 s 的值为 "GOOD MORNING"，调用 split() 方法时没有指定分隔符，因此默认使用空格作为分隔符进行分割，得到的结果是一个包含两个子串的列表 ['GOOD', 'MORNING']。

如果想指定其他分隔符进行分割，可以在 split() 方法中传入分隔符作为参数，例如：

换行符 \n 被视为一个字符，占据一个位置，因此在计算字符串长度时也会被计算在内。

考点：切片操作

此题无输出，因为如果没有指定方向的话，默认从左往右开始切割，而-2是大于-4的明显不对，如果正向切割，那么起始索引要小于结束索引

是把所有的o替换成e

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_python_课程MOC|python MOC]]

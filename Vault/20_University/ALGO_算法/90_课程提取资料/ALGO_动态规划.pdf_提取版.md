---
id: extract-algo-cfe19dfa
type: extract
status: extracted
course: ALGO
source:
  - "[[91_Raw-Archive/PDF/ALGO_动态规划.pdf_课件_未知日期_cfe19dfa.pdf]]"
source_pages: all
source_hash: "cfe19dfa7038e1a935136821e392848733c2c3e23260119fcf258ef8f48975ca"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 动态规划.pdf：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

## 第1页

动态规划  
cover ：小孩不哭站起来
斐波那契数列  
1. 问题描述与递归定义  
斐波那契数列定义为：序列中的每一个数是它前面两个数的和  1 。
当  时，  2222 。
2. 伪代码  ( 递归版本 )  
最长公共子序列  
1. 问题描述  
给定两个序列   和 ，我们要找出它们之间最长的公共子序
列 。
注意 ：子序列不需要连续，只要保持原本的相对顺序即可  。
举例 ： ， 。它们的 LCS 长度为 4 （例如  
） 
2. 状态转移方程  ( 核心模块 )  
我们需要定义一个二维数组   ( 或课件中的  ) ，它表示  序列   的前   个字符和序列   的前   
个字符的最长公共子序列的长度  
根据课件第 22 页，递推公式如下：1. procedure f(n)
2. if (n=1) or (n=2) then return 1
3. else return f(n-1) + f(n-2)
Algorithm LCS-LENGTH(X, Y)
1. m = length[X], n = length[Y]
2. 初始化第一行和第一列为 0  // 对应 c[i,0]=0 和  c[0,j]=0
3. for i = 1 to m do
4.     for j = 1 to n do
5.         if x[i] == y[j] then
6.             c[i, j] = c[i-1, j-1] + 1
7.             b[i, j] = "↖"  // 记录来源，用于回溯
8.         else if c[i-1, j] >= c[i, j-1] then
9.             c[i, j] = c[i-1, j]
10.            b[i, j] = "↑"
11.        else

## 第2页

矩阵链相乘问题  
1. 问题描述  
给定   个矩阵的序列  。
矩阵   的维度为  。
我们要计算它们的连乘积  。
目标 ：由于矩阵乘法满足结合律（即  ），我们可以通过加括号来改变计算次
序。不同的加括号方式会导致完全不同的标量乘法次数（计算成本）。我们要找到一种 加括号的方
式（计算次序） ，使得总的数乘次数 最少  
2. 状态转移方程  ( 核心模块 )  
我们需要定义   为计算矩阵子链   ( 即 ) 所需的 最少 数乘次数  
递推公式如下  ：
解释 ：
1. 基准情况  ( ) ：只有一个矩阵，不需要做乘法，代价为 0 。
2. 递归情况  ( ) ：
我们在   和  之间把链条切断（  是分割点，范围从   到 ）。
总代价  = 左半部分的最优代价   + 右半部分的最优代价   + 这两部分结果矩
阵相乘的代价。
这两部分结果矩阵相乘的代价是： ( 左边结果的行数  )  ( 中间的列数  )  ( 右边结果的
列数  ) 。
我们需要遍历所有可能的切分点  ，取其中的 最小值 。
3. 伪代码  12.            c[i, j] = c[i, j-1]
13.            b[i, j] = "←"
14. return c and b
Algorithm MATRIX-CHAIN-ORDER(p)
1. n = length[p] - 1  // n个矩阵
2. for i = 1 to n do
3.     m[i, i] = 0    // 初始化对角线为 0
4. end for
5. for l = 2 to n do  // l 是链的长度，从 2 开始一直到 n
6.     for i = 1 to n - l + 1 do
7.         j = i + l - 1      // 确定链的终点  j
8.         m[i, j] = ∞        // 初始化为无穷大
9.         for k = i to j - 1 do  // 尝试所有分割点  k
10.            q = m[i, k] + m[k+1, j] + p[i-1]*p[k]*p[j]
11.            if q < m[i, j] then
12.                m[i, j] = q
13.                s[i, j] = k   // 记录最优分割点，用于后续构造解
14.            end if
15.        end for
16.    end for
17. end for

## 第3页

Floyd 求最短路径  
1. 问题描述  
给定一个有向图  ，每条边   有一个非负的长度（权重） 。如果   到  没有边，则
长度为   
目标：我们要找出图中任意两个顶点之间的最短路径距离  
不同于  Dijkstra 算法（它是求一个源点到其他所有点的距离）， Floyd 算法一次性求出所有点对之间的
距离。
2. 状态转移方程  ( 核心模块 )  
我们需要定义   为： 从顶点   到顶点   的最短路径长度，且该路径上除起点和终点外，只允许经
过编号   的中间顶点
递推公式如下：
不经过顶点 经过顶点
解释  ：
这是动态规划的典型思路。假设我们已经知道了只经过前   个点时的最短路径。现在引入第   个
点，对于任意两点   和 ，新的最短路径有两种可能：
1. 不经过  ：路径依然是旧的路径，长度保持   不变。
2. 经过  ：路径变成了 “ 从   走到  ” 加上 “ 从   走到  ” 。长度为  。
我们取这两者中的较小值。
3. 伪代码  
0-1 背包问题  
1. 问题描述  
输入 ：
一个物品集合  。18. return m and s
Algorithm FLOYD(l)
输入: n x n 维矩阵 l[1..n, 1..n]
输出: 矩阵 D，其中 D[i,j] 是 i 到 j 的距离
1. D = l              // 将输入矩阵复制到  D ，初始化  D[i,i]=0, 无边为  ∞
2. for k = 1 to n do  // 核心循环：依次把顶点  k 作为中间跳板
3.     for i = 1 to n do
4.         for j = 1 to n do
5.             D[i, j] = min{ D[i, j], D[i, k] + D[k, j] }
6.         end for
7.     end for
8. end for
9. return D

## 第4页

每个物品   有两个属性： 体积 / 重量  () 和 价值  () 。
一个背包，它的 最大容量 为  。
目标 ：
从这些物品中选出一部分装入背包。
要求：被选物品的 总体积不超过  。
目的：使被选物品的 总价值最大化  。
2. 状态转移方程  ( 核心模块 )  
我们需要定义二维数组  。
含义 ：表示仅从 前  个物品   中选择，放入容量为   的背包中，所能获得的最大价值  。
递推公式如下  ：
详细解释：
这是典型的 “ 选或不选 ” 决策：
1. 装不下  ( ) ：如果当前背包容量   甚至连第   个物品都装不下  ( ) ，那没得选，只能不
装。最大价值等于 “ 前   个物品在容量   下的价值 ” ，即  。
2. 能装下  ( ) ：此时有两种选择，我们取最大值：
不装第   个物品 ：价值依然是  。
装第   个物品 ：此时价值  = 第   个物品的价值   + “ 剩余空间  ( ) 能装的前   个物品
的最大价值 ” 。即  。
3. 伪代码  
 Algorithm KNAPSACK(U, C)
1. for i = 0 to n do
2.     V[i, 0] = 0    // 容量为 0时，价值为 0
3. end for
4. for j = 0 to C do
5.     V[0, j] = 0    // 没有物品时，价值为 0
6. end for
7. for i = 1 to n do  // 遍历每一个物品
8.     for j = 1 to C do  // 遍历每一个容量刻度
9.         V[i, j] = V[i-1, j]   // 先假设不选这个物品
10.        if s[i] <= j then     // 如果装得下
11.       
    // 尝试装入，看是否更划算
12.            V[i, j] = max{ V[i, j], V[i-1, j-s[i]] + v[i] }
13.        end if
14.    end for
15. end for
16. return V[n, C]

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_算法_课程MOC|算法 MOC]]

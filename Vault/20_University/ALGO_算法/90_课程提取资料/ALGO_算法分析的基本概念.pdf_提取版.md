---
id: extract-algo-1886c74b
type: extract
status: extracted
course: ALGO
source:
  - "[[91_Raw-Archive/PDF/ALGO_算法分析的基本概念.pdf_课件_未知日期_1886c74b.pdf]]"
source_pages: all
source_hash: "1886c74b6ddf2f6e50453ecc4156dd44ef9b823d1289697af04b5fd961c70da0"
extract_method: "batch-auto"
created: 2026-07-17
updated: 2026-07-17
verified_by: ""
confidence: medium
needs_review: true
review_reason: "批量自动提取，需人工核对"
---

# 算法分析的基本概念.pdf：提取版

> 本文件是检索中间材料，不是已核对的正式知识结论。

## 第1页

算法分析的基本概念  
cover ：小孩不哭站起来
一、算法的定义  
算法是定义好的能够完成一定任务的有限指令集合，通常它由一个初始化状态，终止于一个对应的
可识别的结束状态。
二、算法的五个特性  
1. 有效性
算法必须在有限的时间能够完成，甚至用纸和笔完成
2. 确定性
算法的每一步能够清楚的定义 .
3. 有限性
算法能够在有限的步骤完成
4. 输入
算法有 0 个或者多个输入
5. 输出
算法有一个或者多个输出
满足有效性，确定性，输入，输出特征的程序是一个过程而不是算法 , 举例：操作系统是过程而不
是算法
三、搜索（用作时间复杂度的引入案例）  
问题描述：  
设A[1..n] 为一个包含 n 个元素的数组 . 考虑这样的问题：判断给定元素 X 是否在 A 中。结果 : 找到这个元素
的下标 j, 1<=j <= n ，使得 x=A[j]; 如果不存在这个元素 ,j=0.
线性搜索：  
扫描数组 A ，依次将每个元素和 x 进行比较 . 
如何经过 j 次比较， 1<=j <= n ，找到 x, x=A[j] ，则返回下标  j;
否则返回下标 0 ，表示搜索失败了 .
伪代码：  
二分搜索：  Input: An array A [1…n] of n elements and an element x. 
Output: j if x = A [j], 1 <= j <= n, and 0 otherwise.
    1. j = 1
    2.while (j < n) and (x != A [j])
    3.  j = j + 1
    4. end while
    5. if x = A [j] then return j else return 0.

## 第2页

设A [low…high] 为一个非递减元素排序的数组 . 其中  A [mid] 表示 A 的中间位置的元素；假设  x>A  
[mid].  
如何在数组 A 中 , 那么 X 在 A [mid] 的后面  
我们进一步在子集 A[mid+1…high] 中搜索 x
伪代码：  
二分搜索算法分析：  
定理 : 规模为 n 的二分搜索算法中元素比较次数最多  . 
什么时候达到最大的比较次数呢？
在while 循环的第 j 次循环时，剩余元素的数目是 。
循环最大次数是满足条件 时的 j 值，因此，   → 
四、时间复杂度  
定义：  
对于任何计算步骤 , 它的代价有一个时间常量作为上界 , 而不管输入数据或执行的算法 , 我们称之为
“ 元运算 ”
注：时间复杂度不是程序运行所花费的实际时间 , 而是对于输入规模 n 的算法元运算的次 .
渐进符号：  
：增长次数小于等于 以及其常数倍的函数集合 —— 渐进上界
直观含义 ：通常用于描述 最坏情况
数学定义： 存在常数   和 ，使得当   时，满足：
                                       
例子 ：
 （主要看最高次项）
 也是对的（因为   确实小于  ），但我们通常习惯找最贴切的
那个上界。
例：证明 100n+5 ∈ O(n2)
当
    取，
当
     取，
：增长次数大于等于 以及其常数倍的函数集合 —— 渐进下界
直观含义 ：通常用于描述 最好情况 或者证明某个问题不可能再快了。Input: 数组A [1…n]包含n个非递减次序的元，查找元素 x. 
Output: j if x = A [j], 1 <= j <= n, and 0 otherwise.
    1.low=1; high = n; j = 0
    2.while (low <= high) and (j = 0)
    3.   mid= low+high/2(取小)
    4.   if x = A [mid] then j =mid
    5.   else if x< A [mid] then high = mid – 1
    6.   else low = mid + 1
    7. end while
    8.return j

## 第3页

数学定义： 存在常数   和 ，使得当   时，满足：
                                       
例子 ：
 （因为   确实大于  ）
：增长次数等于 以及其常数倍的函数集合 —— 渐进紧确界
直观含义 ：算法运行时间的 “ 真实阶数 ” 。当一个算法的 上界 和 下界 是同一个级别时，我们就
用  来表示。这是最精确的描述。
数学定义： 存在常数   和 ，使得当   时，满足：
例子 ：
注意 ： ，因为虽然它小于  ，但它不 “ 等于 ” 的量级。
利用极限比较增长率 :  
 比较两个特定函数的增长率
    
情况①②意味着
情况②③意味着  
情况②意味着  
使用递推关系  
：表示数组或数据实例的大小（元素的个数）。
：表示在大小为   的实例中，执行比较操作的次数。
建立递推公式  (Recurrence Relation)  
这是分析的核心部分，它给出了一个分段函数：
基准情形  ( ) ：如果数组只有一个元素，只需要进行  1 次比较就能确定是否找到，或者确定不
存在。
递归情形  ( ) ：
：表示问题规模缩小了一半。因为二分查找每次都排除掉一半的元素，只在剩下的
一半中继续查找。
：表示当前这一层递归中做了一次比较（将目标值与中间值  mid 进行比较）。
推导过程  (Solving the Recurrence)  
PPT 展示了通过  迭代法（ Substitution Method ） 来解这个递推公式的过程：
1. 第一步 ：
2. 第二步 ：将   带入公式，再次展开  
3. 规律 ：每次展开，问题规模除以  2 ，后面的常数项加  1 。
4. 以此类推 ：经过   次查找后，规模变为  ，比较次数增加  。
最终结论

## 第4页

当问题规模缩小到  1 时停止，即  ，解得  。
因此，总的比较次数公式为：
用大 O 符号表示时间复杂度：

## 提取异常

- 批量自动提取，未经人工核对。

---
> 课程导航：[[../00_算法_课程MOC|算法 MOC]]

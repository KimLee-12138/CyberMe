---
id: extract-db-数据库-student-
type: extract
status: extracted
course: DB
source:
  - "学生笔记（学习资料/数据库/数据库/）"
source_pages: all
extract_method: "direct-read"
created: 2026-07-16
updated: 2026-07-16
verified_by: ""
confidence: medium
needs_review: true
review_reason: "学生笔记需与原始课件核对"
---

# 第一章 数据库基础：学生笔记原文

> 本文件是学生已有笔记的原文保存，需要与原始课件和教材核对。

# 第一章 数据库基础

> cover：小孩不哭站起来（一位24级的神秘人）

## 基础概念

**数据：**	

- **概念：****数据是数据库中存储的基本对象**

- **数据的特点：**数据与其语义是不可分的

- **过程：**收集->保存->抽取有用部分

**数据库**：

- **概念：**数据库**是长期存储在计算机内、有组织的、可共享的大量数据的集合。**
- **基本特征：**数据库中的数据按一定的**数据模型组织、描述和存储**，具有**较小的冗余度、较高的数据独立性和可扩展性**，并可为各种用户共享

**数据库管理系统：**

- **概念：**数据库管理系统（DBMS）**是位于用户与操作系统之间的一层数据管理软件，和操作系统一样是计算机的**基础软件

- **用途：**科学地组织和存储数据，高效地获取和维护数据

- **具体功能：**

  ①**数据定义功能**

​      ②**数据组织、存储和管理功能**

​      ③**数据操纵功能**

​      ④**数据库的事务管理和运行管理功能**

​      ⑤**数据库的建立和维护功能**

​      ⑥**其他功能**

**数据库系统：**

- 概念：数据库系统（DBS）是由**数据库、数据库管理系统、应用程序和数据库管理员（DBA）**组成的存储、管理、处理和维护数据的系统。

## **数据管理技术**

- **概念：**对数据进行分类、组织、编码、存储、检索和维护，是数据处理的中心问题（理解）

- **发展过程：**人工管理->文件系统->数据库系统阶段

  ![image-20251217165440175](C:\Users\Lenovo\Desktop\image-20251217165440175.png)

- **人工管理阶段理(40年代中-50年代中)：**冗余，无共享、不一致、(数据和代码)不独立
- **文件系统(50-60年代)：**和上一结构无本质差别
- **数据库系统(60年代末以来)**

## **数据库系统的特点：**

- 数据结构化
  - **数据库系统实现整体数据的结构化**(和文件系统的本质区别)
  - 数据的最小存取单位是**数据项**。

- 数据的共享性高、冗余度低且易扩展

- 数据独立性高：包括**数据的物理独立性和逻辑独立性**
  - 物理独立性：**应用程序和数据的物理存储方式之间独立**，数据由DBMS隔离管理
  - 逻辑独立性：**应用程序与数据库的逻辑结构之间独立**，数据的逻辑结构改变了，应
    用程序不用改变
- 数据由数据库管理系统统一管理和控制
  - **数据的安全性保护**
  - **数据的完整性检查**
  - **并发控制**
  - **数据库恢复**

## **数据模型：**

- **概念：**数据模型**也是一种模型，**它是对现实时间数据特征的抽象。

- **地位：数据模型是数据库系统的核心和基础。**
- **组成：**数据模型通常由**数据结构**、**数据操作**和**数据的完整性约束条件**三部分组成

## **两个层次**

- **两类数据模型：****概念模型；逻辑模型和物理模型**
- **概念模型：**实际上是**现实世界**到**机器世界**的一个中间层次
- **逻辑模型和物理模型：**
  - 逻辑模型主要包括层次模型、网状模型、关系模型等
  - 逻辑模型不止关系模型，但只需要掌握关系模型即可。
  - 物理模型是对数据最底层的抽象。

## **概念模型：（理解）**

- **实体：**客观存在且可相互区别的事物
- **属性：**实体所具有的某一特性。一个实体可具有多个属性。
- **码：**唯一标识实体的属性
- **实体型：**用实体名及其属性名集合来抽象和刻画，类比编程中“类”的概念
- **联系：**一对一、一对多、多对多

## **概念模型表示方法：**实体-联系方法（E-R图）（理解）

- 详见教材P215
- 用E-R图来描述现实世界的概念模型
- 实体用矩形表示，属性用椭圆框表示
- 联系本身用菱形表示，框内写明联系名，用无向边连接有关实体，并在旁边标上联
  系的类型
- 联系也可以具有属性。

## **逻辑数据模型：**一张规范化的**二维表**

- **关系**（一个关系对应通常说的一张表）
- **元组**（表中的一行即为一个元组）
- **属性**（表中的一列即为一个属性）
- **码**（表中的某个属性组，可以唯一确定一个元组）
- **域**（属性的取值范围）
- **分量**（元组中的一个属性值）

##  **关系的完整性约束条件**

- **实体完整性**

- **参照完整**

- **和用户定义的完整性**

## **数据模型的组成要素：**

- 数据结构：描述数据库的组成对象以及对象之间的联系
- 数据操作：（对型和值）增删改查
- 约束条件：一组完整性规则的集合

## **数据库系统**的结构

- 数据库系统的**三级模式结构**是指数据库系统是由**外模式（用户模式）、模式（逻辑模式）和内模式（存储模式）**三级构成

## **数据库系统的组成**

- **数据库系统**（DBS）**是由**数据库、**数据库管理系统**、应用程序和**数据库管理员**（**DBA**）组成。

---
> 课程导航：[[../00_数据库课程MOC|数据库 MOC]]

"""Generate comprehensive CO documents."""
import os

VAULT = r"C:\Users\22067\Desktop\李尚凯蒸馏\CyberMe-Vault\20_University\CO_计算机组成原理"

docs = {}

# ============================================================
# 计算机系统概述
# ============================================================
docs["02_知识点/计算机系统概述.md"] = """---
id: co-overview
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 计算机系统概述

> 一句话：冯·诺依曼结构五大部件 + 性能评价体系（CPI/MIPS/Amdahl）。

## 一、冯·诺依曼结构

### 1.1 五大部件

| 部件 | 功能 | 硬件实例 |
|------|------|---------|
| 运算器 | 算术/逻辑运算 | ALU、通用寄存器、状态寄存器 |
| 控制器 | 取指、译码、产生控制信号 | PC、IR、控制单元(CU) |
| 存储器 | 存放程序和数据 | 主存(DRAM)、辅存(SSD/HDD) |
| 输入设备 | 将外部数据送入计算机 | 键盘、鼠标、网卡 |
| 输出设备 | 将结果送出计算机 | 显示器、打印机、网卡 |

**核心思想（存储程序原理）**：程序和数据以二进制形式存放在同一存储器中，CPU 逐条取出指令并执行。这是冯·诺依曼结构的精髓。

### 1.2 冯·诺依曼 vs 哈佛结构

| 维度 | 冯·诺依曼 | 哈佛结构 |
|------|----------|---------|
| 指令和数据存储 | 同一存储器 + 同一总线 | 分开的指令存储器/数据存储器 + 分开的总线 |
| 取指与取数 | 不能同时进行（总线冲突） | 可同时取指和取数 |
| 瓶颈 | "冯·诺依曼瓶颈"（总线争用） | 硬件复杂度高 |
| 典型应用 | PC、服务器 | DSP、单片机(如 ARM Cortex-M) |

**现代计算机**：外部统一总线(冯·诺依曼)，CPU 内部有独立的指令 Cache 和数据 Cache(类似哈佛)。

## 二、计算机性能指标

### 2.1 核心公式

$$\\text{CPU执行时间} = \\text{指令数} \\times \\text{CPI} \\times \\text{时钟周期} = \\frac{\\text{指令数} \\times \\text{CPI}}{f}$$

| 符号 | 含义 | 说明 |
|------|------|------|
| $f$ | 时钟频率 | $f = 1/T$，单位 Hz |
| $T$ | 时钟周期 | 一次时钟脉冲的时间 |
| CPI | Cycles Per Instruction | 每条指令平均所需时钟周期数 |
| IC | Instruction Count | 程序的总指令条数 |

### 2.2 CPI 的加权平均

$$\\text{CPI} = \\sum_{i=1}^{n} (\\text{CPI}_i \\times \\frac{\\text{指令数}_i}{\\text{总指令数}})$$

**例**：某程序有 A 类指令(CPI=1)占 40%，B 类(CPI=2)占 40%，C 类(CPI=4)占 20%：
$$\\text{CPI} = 1 \\times 0.4 + 2 \\times 0.4 + 4 \\times 0.2 = 2.0$$

### 2.3 MIPS 和 MFLOPS

$$\\text{MIPS} = \\frac{f}{\\text{CPI} \\times 10^6}$$

$$\\text{MFLOPS} = \\frac{\\text{浮点运算次数}}{\\text{时间(秒)} \\times 10^6}$$

**MIPS 的局限性**：
- 不能跨 ISA 比较（RISC vs CISC 完成相同工作需要不同数量的指令）
- 同一程序在不同编译器优化下 MIPS 可能不同
- 浮点密集型程序可能 MIPS 低但实际性能好

## 三、Amdahl 定律

### 3.1 公式

若改进部分占比为 $f$，该部分加速 $S$ 倍，则系统总加速比：

$$\\text{Speedup}_{\\text{overall}} = \\frac{1}{(1-f) + \\frac{f}{S}}$$

### 3.2 重要推论

**即使对 90% 的部分无限加速($S\\to\\infty$)，整体最多快 10 倍。**

**实践教训**：优化必须针对**瓶颈**——占执行时间最大的部分。

### 3.3 例

某程序 80% 可并行化（$f$=0.8），在 4 核上并行（$S$=4）：
$$\\text{Speedup} = \\frac{1}{(1-0.8) + 0.8/4} = \\frac{1}{0.4} = 2.5$$

## 四、计算机系统的层次结构

```
第5层：高级语言 (C/Java/Python)
         ↓ 编译
第4层：汇编语言
         ↓ 汇编
第3层：操作系统
         ↓
第2层：指令集体系结构(ISA) ← 软硬件分界线
         ↓ 微程序/硬连线
第1层：数字逻辑电路
```

**ISA(指令集体系结构)** 是软硬件之间的"合同"。软件不需要知道下层如何实现。这就是为什么 Intel CPU 的底层微架构代代不同，但所有 x86 软件都能运行。

## 五、易错点

1. **CPI 是加权平均，不是简单平均**
2. **MIPS 不能跨 ISA 比较**（CISC 一条指令可能顶 RISC 好几条）
3. **Amdahl 定律：只改善非瓶颈部分，整体几乎不提升**
4. **时钟频率高 ≠ 性能好**：无法忽视 CPI(Pentium 4 的高频低能就是教训)

---

## 相关链接
- [[数据的机器级表示]] — 冯·诺依曼结构中的数据编码
- [[指令系统]] — ISA 的具体内容
- [[CPU数据通路]] — 如何执行指令
"""
print("Done: 概述")

# ============================================================
# 数据的机器级表示 (THE BIG ONE)
# ============================================================
docs["02_知识点/数据的机器级表示.md"] = """---
id: co-data
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 数据的机器级表示

> 一句话：所有数据都是二进制。原码直观但加减麻烦；补码用模运算统一加减；IEEE 754 用科学计数法表示实数。

## 一、进位计数制与 BCD

- **基数 r**：每位可取 r 个不同数码
- **位权**：第 i 位的权重 = $r^i$
- **BCD 码**：用 4 位二进制表示 1 位十进制。如 15 = 0001 0101(BCD)，而不是 1111(纯二进制)

## 二、原码

**规则**：最高位符号位(0正1负)，其余绝对值。

n 位原码范围：$-(2^{n-1}-1) \\sim +(2^{n-1}-1)$

**致命缺陷**：
1. 0 有两种表示(+0=00...0, -0=10...0)，不利于编程中的相等比较
2. 加减法必须区分符号位单独处理，硬件复杂

## 三、补码(核心中的核心)

### 3.1 模运算思想

时钟模型：模 12 系统中，-4 ≡ +8 (mod 12)。把减法变成加法的补数。

n 位运算器相当于模 $2^n$ 的算盘：**一个负数的补码 = 模 - |该负数|**

### 3.2 补码定义

$$[X]_{\\text{补}} = 2^n + X \\pmod{2^n} \\quad (-2^n \\le X < 2^n)$$

8 位补码范围：-128 ~ +127

### 3.3 重要特性

| 特性 | 说明 |
|------|------|
| **0 的唯一性** | 0 的补码只有 0000 0000(没有负零) |
| **不对称范围** | 负数比正数多一个：-128 有补码(1000 0000)，+128 没有 |
| **符号扩展** | 高位补符号位，真值不变：1111 1111(-1) → 1111 1111 1111 1111(-1) |

### 3.4 补码与真值的互转

**正数**：补码 = 真值的二进制(符号位为 0)
**负数**：真值 → 补码 = 绝对值按位取反+1

**补码转真值**：
- 符号位为 0 → 直接作为正数读
- 符号位为 1 → 按位取反+1，再加负号

**例**：`1111 0000` → 取反=`0000 1111` → +1=`0001 0000`=16 → 真值=-16

**求相反数**：连同符号位，**按位取反+1**。如 5(0000 0101)→-5(1111 1011)

### 3.5 补码加减法

**加法**：$[X+Y]_{\\text{补}} = [X]_{\\text{补}} + [Y]_{\\text{补}} \\pmod{2^n}$

**减法**：$[X-Y]_{\\text{补}} = [X]_{\\text{补}} + [-Y]_{\\text{补}}$

**减法变加法**：减 Y = 加 Y 的相反数的补码！

这是补码的最大优点——**加减法用同一套硬件实现**。硬件只管按位加，不管参与的数原来是正是负。

例：计算 5-3
- 5 的补码：0000 0101
- -3 的补码：1111 1101
- 相加：0000 0101 + 1111 1101 = 1 0000 0010 → 丢弃进位 → 0000 0010 = 2 ✓

## 四、移码

在真值上加偏移量(通常 $2^{n-1}$)，将所有值映射为非负数。

**与补码的关系**：**移码 = 补码的符号位取反**。

**用途**：IEEE 754 浮点数的阶码。因为移码后，指数的自然顺序就是二进制数的自然顺序，便于硬件比较大小。

## 五、无符号整数与带符号整数

| 类型 | n 位范围 | C 类型 |
|------|---------|--------|
| 无符号 | $0 \\sim 2^n-1$ | `unsigned char/short/int` |
| 带符号(补码) | $-2^{n-1} \\sim 2^{n-1}-1$ | `char/short/int` |

**C 语言的隐式类型转换陷阱**：
- 有符号与无符号混合运算 → 隐式转为无符号
- `-1 < 0U` 在 C 中为假！因为 -1 转为无符号后 = 4294967295 > 0

## 六、位扩展与位截断

### 6.1 零扩展(无符号数)
短→长：高位补 0。`0x80`(8位) → `0x0080`(16位)，值不变。

### 6.2 符号扩展(有符号数)
短→长：高位补符号位。`0x80`(8位，=-128) → `0xFF80`(16位，=-128)，值不变。

### 6.3 位截断(长→短)
丢弃高位保留低位。**可能改变值**。

`int` 0x12345678 截为 `short` → 0x5678(高位丢弃)。

## 七、大小端

| 模式 | 低地址存 | 例：0x12345678 @ 地址 0x100 |
|------|---------|------|
| **小端(Little Endian)** | **低字节** | 0x100:78, 0x101:56, 0x102:34, 0x103:12 |
| **大端(Big Endian)** | **高字节** | 0x100:12, 0x101:34, 0x102:56, 0x103:78 |

**口诀**：小端→"小的在低地址"，大端→像人正常读数字(高位在前)。

x86/ARM(默认)是小端；网络字节序(如 IP 地址)是大端。

## 八、IEEE 754 浮点数

### 8.1 浮点数的科学计数法表示

$$N = (-1)^S \\times 1.M \\times 2^{E - \\text{Bias}}$$

- S：符号位(1位)
- M：尾数(隐含了最高位的 1，称为"隐藏位")
- E：阶码(使用移码)

### 8.2 单精度(32位)

| S(1位) | E 阶码(8位) | M 尾数(23位) |
|--------|------------|-------------|

Bias = 127，$E = e + 127$

**例**：10.25 → 1010.01 → $1.01001 \\times 2^3$

S=0, E=3+127=130=1000 0010, M=0100 1000 0000 0000 0000 000

IEEE 754 二进制：`0 10000010 01001000000000000000000`

### 8.3 双精度(64位)

| S(1位) | E 阶码(11位) | M 尾数(52位) |
|--------|-------------|-------------|

Bias = 1023

### 8.4 IEEE 754 特殊值

| 阶码 E | 尾数 M | 表示 |
|--------|--------|------|
| 全 0 | 全 0 | ±0 |
| 全 0 | 非全 0 | 非规格化数(极小值) |
| 全 1 | 全 0 | ±∞ |
| 全 1 | 非全 0 | NaN(Not a Number) |

### 8.5 浮点数的精度

| 精度 | 有效位数 | 范围 |
|------|---------|------|
| 单精度 | ~7 位十进制 | $1.17\\times 10^{-38} \\sim 3.40\\times 10^{38}$ |
| 双精度 | ~16 位十进制 | $2.23\\times 10^{-308} \\sim 1.80\\times 10^{308}$ |

## 九、数据对齐

| 类型 | 大小 | 对齐要求 |
|------|------|---------|
| char | 1 | 任意地址 |
| short | 2 | 2 的倍数 |
| int/float | 4 | 4 的倍数 |
| double/long long | 8 | 8 的倍数 |

**不对齐的代价**：可能需要两次访存 + 拼接，MIPS 直接报异常。

## 十、易错点

1. **补码转真值**：符号位=1 时按位取反+1，再加负号
2. **-128 有补码 1000 0000，但 +128 没有**(8 位)
3. **补码符号扩展后真值不变**，位截断可能改变值
4. **IEEE 754 阶码 = 真值+Bias**，不是真值本身
5. **隐藏位**：规格化尾数隐含一个 1，不存储

---

## 相关链接
- [[运算方法与运算部件]] — 补码运算的硬件实现
- [[计算机系统概述]] — 数据的存储位置
"""
print("Done: 数据表示")

# ============================================================
# 运算方法与运算部件
# ============================================================
docs["02_知识点/运算方法与运算部件.md"] = """---
id: co-alu
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 运算方法与运算部件

> 一句话：ALU 是计算机的"计算心脏"。从加法器到乘法器，从定点运算到浮点运算，都基于最基本的逻辑门。

## 一、基本运算概述

C 语言中涉及的运算：
1. **算术运算**：+-*/ (无符号/带符号/浮点数)
2. **按位运算**：& | ~ ^
3. **逻辑运算**：&& || !
4. **移位运算**：<< >>

## 二、移位运算

| 类型 | 左移(<<) | 右移(>>) |
|------|---------|---------|
| 无符号数 | 低位补 0 | 高位补 0(逻辑右移) |
| 有符号数 | 低位补 0 | **高位补符号位**(算术右移) |

**算术右移 vs 逻辑右移**：
- 算术右移：高位补符号位 → 保持数值的正负(相当于 ÷2)
- 逻辑右移：高位补 0 → 针对无符号数

**例**：`1000 0000`(-128) 算术右移 1 位 = `1100 0000`(-64) ✓

## 三、加法器设计

### 3.1 半加器(Half Adder)
输入：A, B。输出：Sum = A⊕B, Carry = A·B

### 3.2 全加器(Full Adder)
输入：A, B, Cin。输出：Sum = A⊕B⊕Cin, Cout = AB + (A⊕B)Cin

### 3.3 行波进位加法器(Ripple Carry Adder)
n 个全加器串接。简单，但进位传播延迟大 → $O(n)$ 延迟。

### 3.4 超前进位加法器(Carry Look-ahead Adder)
并行计算进位。$G_i = A_i B_i$(生成), $P_i = A_i \\oplus B_i$(传播)
$$C_{i+1} = G_i + P_i C_i$$

延迟 $O(\\log n)$。

## 四、定点乘法

### 4.1 无符号乘法(移位加)

```
乘积初始 = 0
for i = n-1 downto 0:       // 从高位到低位
    乘积 <<= 1
    if 乘数第 i 位 = 1:
        乘积 += 被乘数
```

n 位 × n 位 → 2n 位结果。

### 4.2 Booth 算法(有符号乘法)

对连续的 1 进行"打包"处理：
- 从低位到高位扫描乘数
- `10`(1→0)：减被乘数
- `01`(0→1)：加被乘数
- `00` 或 `11`：不移

减少加/减法次数。

## 五、定点除法

### 5.1 恢复余数法

试商→若余数为负(不够减)→恢复余数→商0→移位→继续。

### 5.2 不恢复余数法(加减交替法)

余数为负时不恢复，直接移位后加(而不是减)。减少一次加法。

## 六、溢出判断

### 6.1 无符号数溢出
最高位产生进位(Cout=1)→溢出。

### 6.2 有符号数溢出(补码)

**三种判断方法**：

1. **进位判断**：符号位进位 Cf != 最高数值位进位 C → 溢出
   $$OF = C_f \\oplus C$$

2. **操作数符号判断**：同号相加得异号 → 溢出

3. **双符号位法**：用 2 位符号位(00正, 11负)，结果两位不同→溢出

### 6.3 例题

8 位补码：`0111 1111`(127) + `0000 0001`(1)
= `1000 0000`(-128) → 正+正=负，OF=1 ✓(溢出！)

## 七、浮点运算

### 7.1 浮点加法步骤

1. **对阶**：小阶向大阶对齐(尾数右移)
2. **尾数相加减**
3. **规格化**：调整尾数和阶码使尾数满足 $1.xxx...$
4. **舍入**：超出精度位按舍入规则处理
5. **溢出检查**

### 7.2 舍入方式

| 方式 | 规则 |
|------|------|
| 截断 | 直接丢弃多余位 |
| 就近舍入(默认) | 舍入到最近的偶数 |
| 向上舍入 | 总是向 +∞ 方向 |
| 向下舍入 | 总是向 -∞ 方向 |

### 7.3 浮点乘法

$N_1 \\times N_2 = (-1)^{S_1\\oplus S_2} \\times (M_1 \\times M_2) \\times 2^{E_1+E_2}$

- 阶码相加(注意减去多余的 Bias)
- 尾数相乘
- 规格化+舍入

## 八、ALU 的结构

```
A ──┐
    ├── MUX ──→ ALU(+-*/&|^) ──→ 结果
B ──┘          ↑
            控制信号(由指令译码产生)
```

ALU 接收两个操作数和控制信号，输出结果和标志位(ZF/CF/OF/SF)。

## 九、易错点

1. **算术右移 ≠ 逻辑右移**：有符号数必须用算术右移
2. **溢出判断**：OF 针对有符号数，CF 针对无符号数
3. **浮点加法先对阶再运算**，不能直接加尾数
4. **Booth 算法扫描是"从低到高"**，不是从高到低

---

## 相关链接
- [[数据的机器级表示]] — 补码与 IEEE 754
- [[CPU数据通路]] — ALU 在数据通路中的位置
"""
print("Done: 运算")

# ============================================================
# Save all done so far + remaining docs
# ============================================================
for rel_path, content in docs.items():
    full_path = os.path.join(VAULT, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content)
    print(f"Written: {rel_path} ({len(content)} chars)")
print(f"\\nTotal: {len(docs)} docs")

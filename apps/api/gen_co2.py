"""Generate remaining CO documents."""
import os

VAULT = r"C:\Users\22067\Desktop\李尚凯蒸馏\CyberMe-Vault\20_University\CO_计算机组成原理"

# ============ 指令系统 ============
open(os.path.join(VAULT, "02_知识点/指令系统.md"), "w", encoding="utf-8").write("""---
id: co-isa
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 指令系统

> 一句话：指令系统(ISA)是软硬件的分界线。它定义了CPU能执行的所有指令及其编码格式。

## 一、指令的基本格式

一条机器指令包含：

| 字段 | 含义 | 说明 |
|------|------|------|
| **操作码(OP)** | 做什么操作 | ADD/SUB/LW/SW/BEQ等 |
| **地址码(A)** | 操作数在哪里 | 寄存器编号或内存地址 |

**指令长度**：定长(如MIPS固定32位)或变长(如x86从1到15字节)。

## 二、寻址方式

### 2.1 基本寻址方式

| 寻址方式 | 含义 | 例(MIPS) |
|---------|------|---------|
| 立即寻址 | 操作数在指令中 | `addi $t0, $zero, 100` |
| 寄存器寻址 | 操作数在寄存器中 | `add $t0, $t1, $t2` |
| 基址寻址 | EA = 基址寄存器 + 偏移量 | `lw $t0, 32($s3)` |
| PC相对寻址 | EA = PC + 偏移量 | `beq $t0, $t1, label` |

### 2.2 寻址方式对比

| . | 优点 | 缺点 |
|----|------|------|
| 立即寻址 | 快(不需访存) | 立即数范围受限 |
| 寄存器寻址 | 快、地址短 | 寄存器数量有限 |
| 基址寻址 | 灵活(数组/结构体) | 需计算有效地址 |
| PC相对寻址 | 位置无关代码 | 跳转范围受限 |

## 三、MIPS 指令集

### 3.1 MIPS 是典型的 RISC

- 32 个 32 位通用寄存器
- 指令定长 32 位
- Load/Store 架构：只有 lw/sw 访问内存
- 三种指令格式：R型、I型、J型

### 3.2 R型指令(寄存器-寄存器)

| op(6) | rs(5) | rt(5) | rd(5) | shamt(5) | funct(6) |
|-------|-------|-------|-------|----------|----------|

例：`add $t0, $t1, $t2` → op=0, funct=0x20

### 3.3 I型指令(立即数)

| op(6) | rs(5) | rt(5) | immediate(16) |
|-------|-------|-------|---------------|

例：`lw $t0, 32($s3)` → op=0x23, rs=$s3, rt=$t0, imm=32

### 3.4 J型指令(跳转)

| op(6) | address(26) |
|-------|-------------|

`j target` → op=0x02

## 四、RISC vs CISC

| 特征 | RISC | CISC |
|------|------|------|
| 指令长度 | **定长**(32位) | **变长**(1-15字节) |
| 指令数量 | 少(几十到一百多) | 多(几百) |
| 寻址方式 | 简单(几种) | 复杂(十几种) |
| 访存指令 | 仅Load/Store | 算术指令也可访存 |
| 代表 | MIPS、ARM、RISC-V | x86 |
| 实现 | 硬连线为主、易流水线 | 微程序为主 |

**现代趋势**：x86 内部将 CISC 指令"翻译"成 RISC 类微操作(μops)。

## 五、常用 MIPS 指令

| 指令 | 格式 | 功能 |
|------|------|------|
| `add $d, $s, $t` | R | $d = $s + $t |
| `sub $d, $s, $t` | R | $d = $s - $t |
| `lw $t, offset($s)` | I | $t = Mem[$s+offset] |
| `sw $t, offset($s)` | I | Mem[$s+offset] = $t |
| `beq $s, $t, label` | I | if $s==$t: PC+=offset<<2 |
| `j target` | J | PC = target |
| `addi $t, $s, imm` | I | $t = $s + imm |

## 六、过程调用约定

| 寄存器 | 用途 |
|--------|------|
| `$a0-$a3` | 传递参数 |
| `$v0-$v1` | 返回值 |
| `$ra` | 返回地址 |
| `$sp` | 栈指针 |
| `$fp` | 帧指针 |
| `$s0-$s7` | 被调用者保存 |
| `$t0-$t9` | 调用者保存 |

## 七、易错点

1. MIPS 的 `offset<<2`：16位立即数左移2位再符号扩展到32位
2. `beq/bne` 使用 PC 相对寻址，范围 ±128KB
3. RISC 必须 Load/Store 才能访存，不能 `add $t0, 100($s0)`

---

## 相关链接
- [[CPU数据通路]] — 指令如何在硬件上执行
- [[流水线技术]] — 如何加速指令执行
- [[数据的机器级表示]] — 指令中数据的编码
""")

# ============ CPU数据通路 ============
open(os.path.join(VAULT, "02_知识点/CPU数据通路.md"), "w", encoding="utf-8").write("""---
id: co-datapath
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# CPU 数据通路

> 一句话：数据通路是 CPU 中数据流动的"高速公路"。控制器产生信号，数据通路执行操作。

## 一、CPU 的基本组成

```
CPU = 数据通路(执行部件) + 控制器(控制部件)

数据通路：ALU、寄存器组、多路选择器(MUX)、总线
控制器：PC、IR(指令寄存器)、指令译码器、控制信号生成逻辑
```

## 二、数据通路的元件

### 2.1 组合逻辑元件(无状态)

- ALU：算数/逻辑运算
- MUX(多路选择器)：从多个输入中选一个
- 加法器：加法运算

### 2.2 时序逻辑元件(有状态，需时钟)

| 元件 | 说明 |
|------|------|
| **寄存器** | 在时钟沿采样输入并保持到下一个时钟沿 |
| **寄存器组** | 多个寄存器组成的阵列(如 32×32位) |
| **PC** | 程序计数器，指向当前指令地址 |

**寄存器组读写**：
- 读：组合逻辑，给出编号→输出数据(不需时钟)
- 写：时序逻辑，WE=1 且时钟沿→写入

## 三、单周期数据通路

### 3.1 设计原则

**一条指令在一个时钟周期内完成**。时钟周期 = 最慢指令的执行时间。

### 3.2 MIPS 单周期数据通路的关键部件

```
取指(IF)：PC→指令存储器→IR
译码(ID)：IR→控制单元→控制信号；寄存器组读出操作数
执行(EXE)：ALU 运算
访存(MEM)：数据存储器读写
写回(WB)：结果写入寄存器组
```

### 3.3 各类指令的数据通路

**R型指令(add/sub/and/or)**：
```
PC→取指→译码(读rs,rt)→ALU(运算)→写回rd
```

**I型访存指令(lw)**：
```
PC→取指→译码(读rs)→ALU(计算地址:rs+imm)→访存(读)→写回rt
```

**I型分支指令(beq)**：
```
PC→取指→译码(读rs,rt)→ALU(比较)→若相等:PC=PC+4+offset<<2
```

## 四、控制器设计

### 4.1 控制信号

| 信号 | 作用 |
|------|------|
| RegDst | 选择写回寄存器(rd/rt) |
| RegWrite | 写使能 |
| ALUSrc | ALU的第二个操作数来源(寄存器/立即数) |
| MemRead | 读存储器 |
| MemWrite | 写存储器 |
| MemtoReg | 写回数据来源(ALU结果/存储器) |
| Branch | 分支指令标志 |
| ALUOp | ALU操作类型 |

### 4.2 硬连线控制器

组合逻辑电路 → 指令操作码输入 → 控制信号输出。**速度快但不易修改**。

### 4.3 微程序控制器

控制信号存在 ROM 中(微程序)，逐条取出执行。**灵活性好但慢**。

## 五、异常与中断

### 5.1 概念
- **异常**：CPU内部产生的(如溢出、未定义指令)→同步
- **中断**：外部设备产生的(如键盘输入)→异步

### 5.2 处理流程
1. 保存当前 PC(存入 EPC 寄存器)
2. 记录异常原因(Cause 寄存器)
3. 跳转到异常处理程序入口
4. 处理完毕用 `eret` 返回

## 六、RTL 描述

RTL(Register Transfer Level)用类似编程的语言描述数据通路行为：

```
// add $t0, $t1, $t2 的 RTL 描述
取指: IR ← Mem[PC]; PC ← PC + 4
译码: A ← R[rs]; B ← R[rt]
执行: ALUOut ← A + B
写回: R[rd] ← ALUOut
```

## 七、易错点

1. **寄存器组读是组合逻辑**(不需要时钟)，**写是时序逻辑**(需要时钟沿)
2. **单周期：时钟周期 = 最慢指令的时间**，浪费资源
3. **lw 指令使用 ALU 计算地址**：$EA = R[rs] + sign\\_extend(imm)$
4. **微程序慢但灵活，硬连线快但不灵活**

---

## 相关链接
- [[指令系统]] — 数据通路执行什么指令
- [[流水线技术]] — 如何改单周期为流水线
""")

# ============ 流水线技术 ============
open(os.path.join(VAULT, "02_知识点/流水线技术.md"), "w", encoding="utf-8").write("""---
id: co-pipeline
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 流水线技术

> 一句话：流水线把指令执行分成多个阶段并行处理，像工厂流水线一样提高吞吐率。

## 一、流水线的基本概念

### 1.1 单周期 vs 流水线

| . | 单周期 | 流水线 |
|----|--------|--------|
| 完成一条指令 | 1 个长周期 | 5 个短周期 |
| CPI | 1 | 理想：1(实际>1) |
| 时钟周期 | 由最慢指令决定 | 由最慢阶段决定(更短) |
| 吞吐率 | 低 | 高 |

### 1.2 MIPS 五级流水

```
IF(取指) → ID(译码) → EXE(执行) → MEM(访存) → WB(写回)
```

各阶段之间用**流水线寄存器**隔开，存储中间结果。

## 二、流水线冒险

### 2.1 结构冒险

多个指令同时使用同一硬件资源。

**解决**：增加硬件(如分离指令Cache和数据Cache)。

### 2.2 数据冒险

后一条指令依赖前一条指令的结果。

**三种情况**：
```
add $t0, $t1, $t2    // $t0 = $t1+$t2
sub $t3, $t0, $t4    // 需要 $t0！但 add 还没写回
```

**解决**：
1. **转发(Forwarding/Bypassing)**：ALU 结果产生后直接转发给下一条指令的 ALU 输入，不等写回寄存器
2. **阻塞(Stall/气泡)**：插入 NOP，等待数据就绪

### 2.3 控制冒险

分支指令跳转时，后续已经进入流水线的指令可能需要被"冲刷"。

**解决**：
1. **假设分支不跳转**：继续取 PC+4
2. **延迟分支**：分支后的指令无论如何都执行
3. **分支预测**：动态预测跳转方向

## 三、流水线性能

### 3.1 加速比

理想加速比 = 流水线段数

$$\\text{Speedup} = \\frac{\\text{非流水线时间}}{\\text{流水线时间}}$$

**实际加速比 < 级数**：流水线寄存器开销、冒险导致的阻塞。

### 3.2 吞吐率

$$\\text{吞吐率} = \\frac{\\text{指令数}}{\\text{总时间}}$$

理想流水线：每个时钟周期完成一条指令。

## 四、流水线寄存器

| 寄存器 | 位置 | 存储内容 |
|--------|------|---------|
| IF/ID | IF→ID | 指令、PC+4 |
| ID/EXE | ID→EXE | 控制信号、操作数、立即数 |
| EXE/MEM | EXE→MEM | ALU结果、写数据、分支目标 |
| MEM/WB | MEM→WB | 读出的数据、ALU结果 |

## 五、数据转发的详细分析

```
add $1, $2, $3     IF ID EXE MEM WB
sub $4, $1, $5        IF ID  EXE MEM WB
```
sub 在 EXE 阶段需要 $1，但 add 在 EXE 阶段就产生了 $1 的结果(比 WB 早两个阶段)。

**转发的关键**：结果一旦在 EXE/MEM 或 MEM/WB 流水线寄存器中可用，立即转发，不等 WB。

**仍需阻塞的情况**：lw 后立即使用。

```
lw $1, 0($2)      IF ID EXE MEM WB
add $3, $1, $4       IF ID  EXE MEM WB
```
lw 的结果在 MEM 阶段才可用，而 add 在 EXE 就需要。必须**阻塞一个周期**。

## 六、易错点

1. **转发不能解决所有数据冒险**：lw 后立即使用仍需阻塞
2. **流水线提高了吞吐率，不是单条指令的执行时间**：单条指令时间不变甚至更慢(流水线寄存器延迟)
3. **流水线级数越多，冒险越复杂**：Pentium 4 的 20+ 级深流水线在分支预测失败时代价巨大

---

## 相关链接
- [[CPU数据通路]] — 单周期数据通路
- [[指令系统]] — 冒险中的指令依赖
""")

# ============ 存储器层次结构 ============
open(os.path.join(VAULT, "02_知识点/存储器层次结构.md"), "w", encoding="utf-8").write("""---
id: co-memory
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: critical
confidence: high
---

# 存储器层次结构

> 一句话：利用局部性原理，用多层存储结构实现"速度接近最快、容量接近最大、价格接近最便宜"的理想存储器。

## 一、存储器的层次

```
CPU寄存器(L0) ← 最快/最小/最贵
    ↓
Cache(L1/L2/L3) ← SRAM
    ↓
主存(DRAM) ← 中等
    ↓
磁盘/SSD ← 最慢/最大/最便宜
```

**局部性原理**：
- **时间局部性**：刚访问的数据很可能再次访问(循环)
- **空间局部性**：刚访问的数据附近的数据很可能被访问(顺序执行)

## 二、SRAM vs DRAM

| . | SRAM | DRAM |
|----|------|------|
| 存储单元 | 6 个晶体管 | 1 个晶体管+1 个电容 |
| 速度 | 快(几ns) | 慢(几十ns) |
| 容量 | 小 | 大 |
| 功耗 | 低(静态) | 需刷新 |
| 用途 | Cache | 主存 |

## 三、Cache 基本原理

### 3.1 映射方式

| 方式 | 规则 | 优点 | 缺点 |
|------|------|------|------|
| **直接映射** | 每个主存块只能放在唯一 Cache 行 | 简单、快 | 冲突多 |
| **全相联** | 可放任意 Cache 行 | 无冲突 | 比较电路复杂 |
| **组相联** | 主存块可放在特定组内任意行 | 折中 | 折中 |

**组相联**最常用。标记(Tag)用于识别块的身份。

### 3.2 Cache 地址分解

$$\\text{地址} = \\text{Tag} | \\text{Index} | \\text{Offset}$$

- **Offset**：块内偏移(Bytes)
- **Index**：行(组)号
- **Tag**：剩余高位 = 标记

### 3.3 替换策略

| 策略 | 规则 |
|------|------|
| LRU | 淘汰最久未使用的(最优) |
| FIFO | 淘汰最早进入的 |
| Random | 随机淘汰 |

### 3.4 写策略

| 策略 | 写操作 | 优点 | 缺点 |
|------|--------|------|------|
| **写直达(Write-through)** | 同时写 Cache 和主存 | 一致性好 | 慢 |
| **写回(Write-back)** | 只写 Cache，替换时写回 | 快 | 需脏位 |

**写不命中时**：
- **写分配(Write-allocate)**：先读入 Cache 再写
- **写不分配(No-write-allocate)**：直接写主存

典型组合：写回+写分配；写直达+写不分配

## 四、Cache 性能

$$\\text{平均访存时间} = \\text{命中时间} + \\text{缺失率} \\times \\text{缺失代价}$$

$$\\text{CPU时间} = \\text{执行周期数} + \\text{访存阻塞周期数}$$

**缺失分类**（3C）：
1. **Compulsory(必然缺失)**：首次访问任何块都缺失
2. **Capacity(容量缺失)**：Cache 装不下所有需要的数据
3. **Conflict(冲突缺失)**：不同块映射到同一 Cache 行

## 五、虚拟存储器

### 5.1 概念
给每个程序一个独立的、连续的虚拟地址空间。通过页表映射到物理地址。

### 5.2 分页

- **虚拟页号(VPN)** → **物理页帧号(PPN)**
- **页大小**：通常 4KB
- **页表**：存在主存中；每个进程一张页表

### 5.3 TLB(快表)

页表的 Cache。避免每次访存都查页表(查两次：页表+实际数据)。

TLB 命中 → 直接得到物理地址 → 一次访存
TLB 缺失 → 查页表 → 可能需要两次访存

### 5.4 虚实地址转换

$$\\text{虚拟地址} = \\text{VPN} | \\text{offset}$$

$$\\downarrow \\text{TLB/Page Table}$$

$$\\text{物理地址} = \\text{PPN} | \\text{offset}$$

## 六、存储层次总结

| 层次 | 典型大小 | 延迟 |
|------|---------|------|
| L1 Cache | 32KB | ~1ns |
| L2 Cache | 256KB | ~5ns |
| L3 Cache | 8MB | ~15ns |
| 主存(DRAM) | 8GB | ~50ns |
| SSD | 512GB | ~100μs |
| HDD | 2TB | ~10ms |

## 七、易错点

1. **Cache 映射中 Index 决定行号，Tag 决定是否命中**
2. **写回**：替换脏块时必须先写回主存
3. **TLB 本质是"页表的 Cache"**
4. **虚拟地址到物理地址的转换发生在 Cache 访问之前**(物理Cache)或之后(虚拟Cache)

---

## 相关链接
- [[计算机系统概述]] — 性能分析
- [[输入输出系统]] — DMA与存储
""")

# ============ 输入输出系统 ============
open(os.path.join(VAULT, "02_知识点/输入输出系统.md"), "w", encoding="utf-8").write("""---
id: co-io
type: concept
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
mastery: reviewing
importance: high
confidence: high
---

# 输入输出系统

> 一句话：I/O 系统是计算机与外部世界交互的桥梁。三种控制方式——程序查询、中断、DMA——速度递增、CPU参与递减。

## 一、I/O 接口

### 1.1 功能
- 数据缓冲(速度匹配)
- 信号转换(电平/格式)
- 地址译码(选中设备)
- 提供状态信息

### 1.2 I/O 端口编址

| 方式 | 说明 | 代表 |
|------|------|------|
| **独立编址** | I/O 端口有独立地址空间 | x86(in/out指令) |
| **统一编址** | I/O 端口映射到内存地址空间 | ARM、MIPS(Load/Store访问) |

## 二、三种 I/O 控制方式

### 2.1 程序查询(轮询)

```
while (设备状态 != 就绪):
    等待
传送数据
```

**特点**：CPU 全程参与，**CPU 利用率极低**("忙等"或称自旋)。适合简单低速设备。

### 2.2 中断方式

CPU 启动 I/O 后继续执行其他程序。I/O 完成时设备发出中断信号。

**流程**：
1. 设备完成 I/O → 发中断
2. CPU 响应 → 保存现场 → 执行中断服务程序
3. 中断返回 → 继续原程序

**优势**：CPU 与 I/O 可并行工作。

### 2.3 DMA(直接存储器访问)

**硬件(DMA控制器)直接完成设备与主存的数据传送，CPU 完全不参与数据搬运。**

**DMA 工作流程**：
1. CPU 初始化 DMA 控制器(源/目的地址、传送字节数)
2. DMA 控制器接管总线，直接传送数据
3. 传送完毕，DMA 发中断通知 CPU

**DMA 传送模式**：
- **突发模式**：连续传送一块数据(CPU 等待)
- **周期窃取**：在 CPU 不使用总线时插空传送

## 三、中断的详细处理

### 3.1 中断类型

| 类型 | 来源 | 例子 |
|------|------|------|
| **可屏蔽中断** | 外部设备 | 键盘、鼠标 |
| **非屏蔽中断** | 严重硬件故障 | 电源掉电 |
| **异常** | CPU内部 | 除法溢出、缺页 |
| **软中断** | 程序主动 | `int 0x80` |

### 3.2 中断向量表

存放每个中断号对应的处理程序入口地址。

### 3.3 中断优先级

多个中断同时到达时，CPU 先响应优先级高的。高优先级中断可以**嵌套**打断低优先级的中断处理。

## 四、总线

### 4.1 总线类型

| 总线 | 功能 |
|------|------|
| 数据总线 | 传送数据(双向) |
| 地址总线 | 发出内存/I/O地址(单向) |
| 控制总线 | 发送控制信号 |

### 4.2 总线仲裁

| 方式 | 说明 |
|------|------|
| 菊花链 | 设备串行连接，优先级从近到远 |
| 集中式仲裁 | 仲裁器决定谁使用总线 |
| 分布式仲裁 | 各设备自行判断 |

### 4.3 现代总线标准

- **PCIe**：串行点对点，取代并行 PCI
- **USB**：通用串行总线
- **SATA**：硬盘接口

## 五、易错点

1. **DMA 接管总线时 CPU 不能使用总线**(周期窃取时交替)
2. **中断响应过程保存的是 PC 和状态寄存器**(不是所有寄存器，那由中断服务程序来做)
3. **独立编址 vs 统一编址**：x86用in/out，ARM/MIPS用Load/Store

---

## 相关链接
- [[CPU数据通路]] — 异常处理的数据通路
- [[存储器层次结构]] — DMA 与主存交互
""")

# ============ 公式速查 ============
open(os.path.join(VAULT, "03_公式与规则/计组公式速查.md"), "w", encoding="utf-8").write("""---
id: co-formula-ref
type: reference
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
---

# 计组公式速查

## 一、性能

- CPU时间 = 指令数 × CPI × 时钟周期 = (指令数 × CPI) / f
- CPI = Σ(CPIi × 比例i)
- MIPS = f / (CPI × 10^6)

## 二、Amdahl 定律

Speedup = 1 / ((1-f) + f/S)

## 三、Cache

- 平均访存时间 = 命中时间 + 缺失率 × 缺失代价
- 地址 = Tag | Index | Offset
- Cache 行数 = 容量 / (块大小 × 相联度)

## 四、浮点数(IEEE 754)

单精度 Bias=127：N = (-1)^S × 1.M × 2^(E-127)
双精度 Bias=1023：N = (-1)^S × 1.M × 2^(E-1023)

## 五、流水线

加速比 ≈ 流水线段数 (理想)
吞吐率 = 指令数 / 总时间

## 六、补码

[X]补 = 2^n + X (mod 2^n)
-128的8位补码 = 1000 0000(没有对应正数)

## 七、存储容量

n 位地址总线 → 寻址能力 = 2^n 单元
存储容量 = 2^地址线数 × 数据线位宽
""")

# ============ 错题记录 ============
open(os.path.join(VAULT, "04_例题与错题/错题记录_计组.md"), "w", encoding="utf-8").write("""---
id: co-error-log
type: tracker
status: active
course: CO
created: 2026-07-17
updated: 2026-07-18
---

# 计组错题记录

## #1：CPI 是加权平均不是简单平均
□ □ □

## #2：-128 的补码是 1000 0000(8位)
□ □ □

## #3：IEEE 754 阶码 = 真值 + Bias(Bias单精度=127)
□ □ □

## #4：Cache 直接映射 Tag|Index|Offset 地址分解
□ □ □

## #5：写回法需要脏位，写直达法不需要
□ □ □

## #6：流水线转发不能消除 lw 后的数据冒险
□ □ □

## #7：Amdahl 定律：优化非瓶颈部分提升很小
□ □ □

## #8：MIPS 不能跨 ISA 比较(CISC vs RISC)
□ □ □

## #9：大小端：小端低字节在低地址(x86)，大端反之
□ □ □
""")

print("All CO docs generated!")
print(f"Total: ~{sum(len(open(os.path.join(VAULT, p), encoding='utf-8').read()) for p in ['02_知识点/'+f for f in ['计算机系统概述.md','数据的机器级表示.md','运算方法与运算部件.md','指令系统.md','CPU数据通路.md','流水线技术.md','存储器层次结构.md','输入输出系统.md']] + ['03_公式与规则/计组公式速查.md','04_例题与错题/错题记录_计组.md'])} chars")
